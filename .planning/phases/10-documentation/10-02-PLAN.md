---
phase: 10-documentation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - README.md
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "A new SE can follow the README to install the repo, configure credentials, and run their first skill"
    - "A new SE can find any skill by category and see its invocation, required credentials, and a usage example"
    - "A new SE can onboard a new customer by following the step-by-step walkthrough with concrete commands"
    - "A new SE can understand how the dashboard pipeline works from data sources through to browser output"
  artifacts:
    - path: "README.md"
      provides: "Single-document entry point for all project documentation"
      contains: "## Quick Start"
    - path: "README.md"
      provides: "Skill reference with categories and examples"
      contains: "## Skills"
    - path: "README.md"
      provides: "Customer onboarding walkthrough"
      contains: "## Customer Onboarding"
    - path: "README.md"
      provides: "Architecture overview with Mermaid diagrams"
      contains: "## Architecture"
  key_links:
    - from: "README.md Skills section"
      to: "SKILL-INVENTORY.md"
      via: "markdown link"
      pattern: "SKILL-INVENTORY.md"
    - from: "README.md Workflow Patterns section"
      to: ".claude/rules/skill-composition.md"
      via: "markdown link"
      pattern: "skill-composition.md"
    - from: "README.md Quick Start"
      to: "~/.fe-skills/.env"
      via: "credential path in code block"
      pattern: "~/.fe-skills/.env"
    - from: "README.md Architecture"
      to: "assemble.py -> compose.py pipeline"
      via: "Mermaid diagram"
      pattern: "```mermaid"
---

<objective>
Rewrite README.md as the single front door for all project documentation, covering getting started, skill reference, customer onboarding, and architecture overview.

Purpose: A new W&B SE should be able to go from zero to running their first skill and generating a customer dashboard by following this one file. This is the capstone deliverable of the v2.0 milestone.
Output: Enhanced README.md with all four DOCS requirements satisfied, using the renamed ~/.fe-skills/ credential path from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-01-SUMMARY.md

@README.md
@SKILL-INVENTORY.md
@.claude/rules/skill-composition.md
@templates/customers.yaml
@.claude/rules/asana.md
</context>

<interfaces>
<!-- Key files the executor needs for content sourcing. -->

From SKILL-INVENTORY.md:
- Full 35-skill table with Type, Required Credentials, Invocation columns
- Dependency graph section
- Composition Workflows table

From templates/customers.yaml (header comments):
- Schema documentation: name, sfdc_account_id, jira_customer, slack_channels, action_tracker, raid_tracker, portfolio_id, deployment_type, cadence, contacts

From .claude/rules/skill-composition.md:
- 15 named workflows: Issue Triage, Customer Lookup, Customer Snapshot, Dashboard Generation, Communication Prep, FE-UPDATE Workflow, Action Tracking, Programme Update, RAID Management, Usage Report, Customer Onboarding, Customer Silence Check, Meeting Follow-Up, Task Hygiene, Lattice Weekly Update

From .claude/skills/customer-snapshot/ (dashboard pipeline):
- assemble.py: accepts --jira, --bq, --asana, --sentiment JSON files + --customer, --days, --months, --audience flags. Outputs INTELLIGENCE_DATA JSON.
- compose.py: accepts --customer, --data, --output. Reads panels.yaml, copies shell.html, writes data.js, copies panel JS files to panels/, bundles lib/.
- panels.yaml: manifest of all panels with id, file, data_key, requires fields.
- PanelRegistry contract: register(id, {render, getHeadlineStats, getAttentionItems})
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with getting-started, skill reference, customer onboarding, and architecture overview</name>
  <files>README.md</files>
  <read_first>
    README.md,
    SKILL-INVENTORY.md,
    .claude/rules/skill-composition.md,
    templates/customers.yaml,
    .claude/rules/asana.md,
    .claude/skills/customer-snapshot/SKILL.md,
    .claude/skills/customer-snapshot/templates/panels.yaml
  </read_first>
  <action>
    Rewrite README.md as a single comprehensive document per D-01, D-02. Keep the existing content as the foundation and enhance/reorganize it. The credential path must be `~/.fe-skills/.env` throughout (Plan 01 already renamed it). Target audience is CLI-comfortable per D-06 -- no explaining what uv or git clone means.

    The README should have these sections in this order:

    **1. Header and intro** (existing, minor update)
    - Keep "W&B Field Engineering Skills" title
    - Keep the one-liner description and live systems note
    - Update skill count if needed (currently says 29, should be 35)

    **2. Quick Start** (DOCS-01 -- enhance existing)
    - Step 1: Clone + install uv (just the commands)
    - Step 2: Configure credentials in `~/.fe-skills/.env` (keep the existing env var block, already comprehensive)
    - Step 3: Run credential health check: `/credential-status`
    - Step 4: Run setup skills for each service: `/atlassian-setup`, `/slack-setup`, etc.
    - Step 5: Install Python deps: show `uv sync` for the skills that need it
    - Step 6: First skill run -- suggest `/jira list --customer GResearch` as the hello-world
    - Step 7: Global install (symlink pattern, already exists)
    - Focus depth on W&B-specific parts per D-07: which tokens come from where (Atlassian API tokens page, Slack cookie extraction, Asana PAT page, Salesforce OAuth flow, Apps Script deployment URLs)

    **3. Skills** (DOCS-02 -- enhance existing)
    - Keep the 4 existing table categories: Customer Engagement, Asana Action Tracking, Data Sources, Setup & Diagnostics
    - Add missing skills to tables if any are absent (cross-check against SKILL-INVENTORY.md's 35 entries)
    - Add a new category table for **Reporting & Analytics**: deep-analytics, usage-report, customer-snapshot, lattice, 3p-update, rats
    - For each category, add 1-2 concrete usage examples after the table showing realistic invocations with expected output descriptions
    - Add a line linking to SKILL-INVENTORY.md: "For the full flat inventory with dependency graph and credential matrix, see [SKILL-INVENTORY.md](SKILL-INVENTORY.md)." per D-03

    **4. Workflow Patterns** (existing, minor update)
    - Keep existing list but add Dashboard Generation and Lattice Weekly Update workflows (added in Phase 9)
    - Link to `.claude/rules/skill-composition.md` for full workflow details

    **5. Customer Onboarding** (DOCS-03 -- new section)
    - Worked example with a fictional customer "Acme Corp" per D-10, D-11
    - Step-by-step flow with concrete commands and expected output at each stage:
      a. Look up the customer in Salesforce: `/salesforce account-search --name "Acme Corp"` -- note the 18-char account ID
      b. Run customer setup: `/customer-setup "Acme Corp"` -- this creates customers.yaml entry, prompts for Slack channels, deployment type
      c. Verify customers.yaml entry: show what the YAML block looks like after setup
      d. Asana portfolio setup: `/asana setup-customer --name "Acme Corp" --master-portfolio-gid 1213799845117811` -- creates portfolio + Actions + RAID projects
      e. Update customers.yaml with Asana GIDs (action_tracker_id, raid_tracker_id, portfolio_id)
      f. First dashboard generation: `/customer-snapshot "Acme Corp"` -- generates dashboard folder
      g. Open in browser: `open customers/acme-corp/dashboard/index.html`
    - Include the customers.yaml schema reference (name, sfdc_account_id, jira_customer, slack_channels, action_tracker/id, raid_tracker/id, portfolio_id, deployment_type, cadence, contacts) sourced from the header comments in templates/customers.yaml

    **6. Architecture** (DOCS-04 -- new section)
    - Use Mermaid diagrams per D-08
    - Dashboard pipeline Mermaid diagram per D-09 showing:
      ```
      Jira API --> assemble.py --> INTELLIGENCE_DATA JSON --> compose.py --> dashboard/
      BQ API ---/                                                          |-> index.html
      Slack API /                                                          |-> data.js
      Asana API/                                                           |-> panels/*.js
                                                                           |-> lib/
      ```
    - Panel contract explanation: PanelRegistry.register(id, {render, getHeadlineStats, getAttentionItems}). Panels are individual JS files in panels/ directory. Shell loads them on demand via script injection. Each panel gets the full INTELLIGENCE_DATA object.
    - Data flow narrative: "When you run `/customer-snapshot CustomerName`, the skill: (1) fetches Jira issues, BQ usage data, Asana tasks, and Slack sentiment in parallel, (2) feeds all data into assemble.py which runs analytics transforms and produces a single INTELLIGENCE_DATA JSON object, (3) passes that JSON to compose.py which reads panels.yaml to determine which panels have data, copies shell.html + active panel JS files + lib/ into a dashboard folder, and writes data.js, (4) opens the dashboard in your browser."
    - How to add a new panel (brief): create a panel JS file implementing the PanelRegistry contract, add an entry to panels.yaml, add the data key to assemble.py transforms.
    - Second Mermaid diagram: panel lifecycle showing shell.html -> loads panel-registry.js -> reads panels.yaml -> injects panel script tags -> each panel calls PanelRegistry.register() -> shell calls render() for active panel

    **7. Asana Structure: RAID Two-Project Model** (existing, keep as-is)

    **8. Project Structure** (existing, update skill count from 29 to 35)

    **9. Credentials** (existing, update path from ~/.tsm-ai/.env to ~/.fe-skills/.env -- should already be done by Plan 01, just verify)

    **10. Customer Registry** (existing, keep as-is)

    **11. Python Skills** (existing, keep as-is)

    Writing style: clear, direct, no filler. Commands should be copy-pasteable. Every section should help a new SE do something, not just understand something.
  </action>
  <verify>
    <automated>cd /Users/astevenson/gitstuff/field-eng-skills && grep -c '## Quick Start' README.md && grep -c '## Skills' README.md && grep -c '## Customer Onboarding' README.md && grep -c '## Architecture' README.md && grep -c 'mermaid' README.md && grep -c 'SKILL-INVENTORY.md' README.md && grep -c 'fe-skills' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Quick Start` section (grep returns 1)
    - README.md contains `## Skills` section (grep returns 1)
    - README.md contains `## Customer Onboarding` section (grep returns 1)
    - README.md contains `## Architecture` section (grep returns 1)
    - README.md contains at least one mermaid code block (grep 'mermaid' returns >= 1)
    - README.md links to SKILL-INVENTORY.md (grep returns >= 1)
    - README.md references ~/.fe-skills/.env not ~/.tsm-ai/.env (grep 'fe-skills' returns >= 3, grep 'tsm-ai' returns 0)
    - README.md contains "Acme Corp" customer onboarding example (grep 'Acme Corp' returns >= 1)
    - README.md contains `PanelRegistry` in architecture section (grep 'PanelRegistry' returns >= 1)
    - README.md contains `assemble.py` and `compose.py` in architecture section
    - README.md contains `panels.yaml` reference
    - README.md contains at least 4 skill category tables (grep '| Skill' returns >= 4)
    - README.md total line count is between 350 and 700 lines (substantial but not bloated)
  </acceptance_criteria>
  <done>README.md is a comprehensive single-document entry point covering getting started, skill reference with examples, customer onboarding walkthrough, and architecture overview with Mermaid diagrams. All credential paths reference ~/.fe-skills/.env.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review README and run humanizer</name>
  <files>README.md</files>
  <action>
    Present README.md for user review. User will:
    1. Read through each section for accuracy and completeness
    2. Verify Mermaid diagrams render correctly
    3. Run `/humanizer` on the README content per D-12 to remove AI writing patterns
    4. Apply any edits from humanizer output
    This is a human verification checkpoint -- no automated action needed.
  </action>
  <verify>
    <automated>wc -l README.md</automated>
  </verify>
  <done>User has reviewed README.md, humanizer pass is complete, and content is approved for commit.</done>
</task>

</tasks>

<verification>
1. README.md has all 4 required sections: Quick Start, Skills, Customer Onboarding, Architecture
2. At least one Mermaid diagram present and syntactically valid
3. Links to SKILL-INVENTORY.md and skill-composition.md are present
4. All credential paths reference ~/.fe-skills/.env (zero tsm-ai references)
5. Customer onboarding example uses concrete commands with expected output
6. Architecture section explains assemble.py -> compose.py pipeline and PanelRegistry contract
</verification>

<success_criteria>
- A new W&B SE can follow the README from clone to first dashboard generation without asking questions
- All 4 DOCS requirements (DOCS-01, DOCS-02, DOCS-03, DOCS-04) are satisfied in a single README.md
- Humanizer pass completed per D-12
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-02-SUMMARY.md`
</output>
