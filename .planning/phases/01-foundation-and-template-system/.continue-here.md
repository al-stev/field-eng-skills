---
phase: 02-high-confidence-pages
task: null
total_tasks: null
status: in_progress
last_updated: 2026-03-25T13:57:06.819Z
---

<current_state>
Phase 1 (Foundation) complete. Phase 2 in progress — Feature Velocity page is done and committed with real BQ data. 3 pages remaining in Phase 2: SDK Versions, Engagement Decay, User Journey. No GSD plans created for Phase 2 yet — we've been building directly against real data after the user pushed back on fake data and overly formal phasing.
</current_state>

<completed_work>

- Phase 1 Foundation: All 4 plans executed (BQ guardrails, skill scaffold, base template, integration tests)
- Feature Velocity page: Full pipeline — BQ query → transform → HTML with sparkline grid
  - V4 design: gold bars + event labels above + green user counts below
  - Real AI narrative from data patterns (highlights, recommendations, cliff detection)
  - Weave suppression via account_health_query
  - Fixed product_areas_query ghost user bug (event_count > 0 filter)
  - Tested with Isomorphic Labs (dedicated-cloud) — surfaced real Jan 22 collaboration cliff
</completed_work>

<remaining_work>

- SDK Versions page (Phase 2) — cli_version/local_version from ext_daily_user_event_usage, donut chart + version table
- Engagement Decay page (Phase 2) — cold-detection table ranking users by activity decline, per-user sparklines
- User Journey page (Phase 2) — Sankey from dim_users first_*_at fields, stage completion counts
- Phase 3: Cohort Analysis, Team Detection, Risk Scoring (medium-confidence data)
- Phase 4: Usage Correlation (privacy-sensitive), Performance Deep Dive (exploratory)
- Data provenance indicator (deployment type / source) on all pages
- Clean up V1-V3 dead renderer code in base-template.html
- Meta skill to orchestrate all pages
</remaining_work>

<decisions_made>

- V4 sparkline design (numbers, not dots/lines) — iterated through 4 options, user preferred event count labels above bars + user count below in green
- Always use real BQ data for prototyping — user rejected fake/sample data as useless
- Weave suppression for non-Weave customers, but show with note if there's usage (upsell signal)
- Collaboration (team_or_profile_viewed) may need revisiting — broad event, plus Isomorphic Labs has a customer-specific cliff on Jan 22 2026
- Each new page follows the pattern: handler in generate.py → transform class → renderer in template JS → PAGE_RENDERERS entry
</decisions_made>

<blockers>
- None blocking. Isomorphic Labs collaboration cliff is an open investigation (customer-specific, not platform-wide).
</blockers>

<context>
The user is an SE who wants these pages to be directly usable in customer conversations. Every piece of data must be trustworthy and clearly labeled. The iterative design loop on Feature Velocity was valuable — expect similar iteration on each new page type. The user has strong opinions on data accuracy and provenance. Don't show anything misleading; flag uncertainty explicitly.

The remaining Phase 2 pages are independent — build in any order. SDK Versions is the fastest win. Engagement Decay is the highest SE value. User Journey (Sankey) is the most visually impressive.

BQ queries cost ~36GB per run against ext_daily_user_event_usage. Use maximum_bytes_billed=50_000_000_000 (or 100B for dim_users joins). The account_health_query is cheaper at ~5GB.
</context>

<next_action>
Start with: Build SDK Versions page following Feature Velocity pattern — add sdk_versions_query() to queries.py, create SdkVersionsTransform, add renderSdkVersions to template, generate for Isomorphic Labs, iterate with user.
</next_action>
