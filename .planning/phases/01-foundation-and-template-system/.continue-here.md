---
phase: 02-high-confidence-pages
task: null
total_tasks: null
status: complete
last_updated: 2026-03-26T13:23:49.858Z
---

<current_state>
Phase 2 (High-Confidence Pages) is COMPLETE. All 4 pages built with real BQ data, iterated visually with user, committed. Phase 3 is being built in a parallel session. Phase 4 (Usage Correlation + Performance Deep Dive) is next for this session track.
</current_state>

<completed_work>

- Phase 1 Foundation: All 4 plans executed (BQ guardrails, skill scaffold, base template, integration tests)
- Feature Velocity page: Sparkline grid with momentum, Weave suppression, cliff detection (c2912d9)
- SDK Versions page: Donut + stacked bar timeline + version table with freshness badges (ce88aff)
  - Fixed N/A handling in cli_version, comma-separated local_version parsing
  - 14 versions tracked for Isomorphic Labs, 99% on latest (0.78)
- Engagement Decay page: Per-user sparklines + ranked table with filters (ce88aff)
  - Licensed filter (SFDC contracted_seats) defaults ON — top N users by activity
  - Interactive status toggles (hot/warm/cooling/cold) with dynamic counts
  - Identity resolution: username > email local-part > user-ID fallback
  - Hot-first sort, no user cap (238 users for Isomorphic Labs)
- User Journey page: ECharts Sankey + funnel bars from dim_users milestones (5828608)
  - Auto-suppresses empty stages (license for server deployments, weave for non-Weave)
  - 320 users → 298 SDK → 202 runs → 180 tables / 53 sweeps for Isomorphic Labs
</completed_work>

<remaining_work>

- Phase 3: Cohort Analysis, Team Detection, Risk Scoring (IN PROGRESS — parallel session)
- Phase 4: Usage Correlation (cross-account, privacy-sensitive), Performance Deep Dive (LOW confidence)
  - Needs: /gsd:ui-phase then /gsd:plan-phase before execution
  - fct_application_performance table access unverified — may need descoping
- Data provenance indicator (deployment type / source) on all pages — deferred XCUT
- Clean up V1-V3 dead renderer code in base-template.html
- Meta skill to orchestrate generating all pages at once
</remaining_work>

<decisions_made>

- SDK versions query: NULLIF for N/A strings, SPLIT for comma-separated local_version — server deployments don't populate cli_version cleanly
- Engagement decay: licensed filter defaults ON — SE wants licensed users by default (238 tracked vs 183 licensed)
- Engagement decay: hot-first sort, no user cap — cold-first with 50 cap hid all hot/warm/cooling users
- Identity resolution: prefer username > email local-part > user-ID — some server users have email but no username
- User Journey: auto-suppress empty stages (license=0 for server deploys, weave=0 for non-Weave customers)
- Each page follows pattern: query in queries.py → transform class → handler in generate.py → renderer in base-template.html → PAGE_RENDERERS entry
</decisions_made>

<blockers>
- Phase 4 Performance Deep Dive depends on fct_application_performance table — LOW data confidence, may not be accessible
- Phase 4 Usage Correlation needs cross-account data — privacy-sensitive, SE-internal only
- No blockers for Phase 3 (parallel session)
</blockers>

<context>
User is an SE building these pages for real customer conversations. Every visualization was iterated against live Isomorphic Labs BQ data. The user has strong opinions on data accuracy — never show misleading data. The licensed-user filter was a key user request: engagement decay showed 238 users but only 183 are licensed, so the default view filters to licensed.

Phase 3 is running in a separate session. If starting Phase 4 here, begin with /gsd:ui-phase to spec the two remaining pages (Usage Correlation + Performance), then /gsd:plan-phase to plan execution. Skip /gsd:discuss-phase since the user said no discussion needed.
</context>

<next_action>
For Phase 4: Start with /gsd:ui-phase for Phase 4, then /gsd:plan-phase. Or wait for Phase 3 to complete first. The user mentioned wanting to do Phase 4 in this session track but will start a new session.
</next_action>
