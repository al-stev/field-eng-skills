---
phase: 07-jira-instance-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/jira/scripts/jira_client.py
  - .claude/skills/jira/SKILL.md
  - .claude/skills/credential-status/scripts/check.sh
  - .claude/rules/atlassian.md
  - .claude/skills/atlassian-setup/SKILL.md
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [JIRA-01, JIRA-04]

must_haves:
  truths:
    - "Jira API client connects to coreweave.atlassian.net instead of wandb.atlassian.net"
    - "Credential health check validates against coreweave.atlassian.net"
    - "All project documentation references coreweave.atlassian.net as the Jira instance"
    - "Atlassian rules file directs agents to coreweave.atlassian.net"
  artifacts:
    - path: ".claude/skills/jira/scripts/jira_client.py"
      provides: "Jira API client with updated JIRA_SERVER constant"
      contains: "coreweave.atlassian.net"
    - path: ".claude/skills/credential-status/scripts/check.sh"
      provides: "Health check hitting new Jira instance"
      contains: "coreweave.atlassian.net"
    - path: ".claude/rules/atlassian.md"
      provides: "Agent rules pointing to new Jira instance"
      contains: "coreweave.atlassian.net"
    - path: "CLAUDE.md"
      provides: "Project instructions with updated credential table"
      contains: "coreweave.atlassian.net"
  key_links:
    - from: ".claude/skills/jira/scripts/jira_client.py"
      to: "coreweave.atlassian.net"
      via: "JIRA_SERVER constant used by get_client()"
      pattern: 'JIRA_SERVER.*coreweave\.atlassian\.net'
    - from: ".claude/skills/credential-status/scripts/check.sh"
      to: "coreweave.atlassian.net"
      via: "curl health check URL"
      pattern: 'coreweave\.atlassian\.net/rest/api/3/myself'
---

<objective>
Migrate the core Jira integration layer and all project-level documentation from wandb.atlassian.net to coreweave.atlassian.net.

Purpose: This is the foundation of the migration -- the API client that all Jira operations flow through, the credential health checker, the agent rules, and the canonical project documentation. Everything downstream depends on these being correct first.

Output: jira_client.py connecting to the new instance, check.sh validating against it, and all docs/rules updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-jira-instance-migration/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Jira API client, health checker, and rules</name>
  <files>
    .claude/skills/jira/scripts/jira_client.py
    .claude/skills/credential-status/scripts/check.sh
    .claude/rules/atlassian.md
    .claude/skills/atlassian-setup/SKILL.md
  </files>
  <read_first>
    .claude/skills/jira/scripts/jira_client.py
    .claude/skills/credential-status/scripts/check.sh
    .claude/rules/atlassian.md
    .claude/skills/atlassian-setup/SKILL.md
    .planning/phases/07-jira-instance-migration/07-CONTEXT.md
  </read_first>
  <action>
    Per D-01: Straightforward URL swap from wandb.atlassian.net to coreweave.atlassian.net.

    1. In `.claude/skills/jira/scripts/jira_client.py`:
       - Change line 20: `JIRA_SERVER = "https://wandb.atlassian.net"` to `JIRA_SERVER = "https://coreweave.atlassian.net"`
       - No other changes needed -- the client uses JIRA_SERVER throughout

    2. In `.claude/skills/credential-status/scripts/check.sh`:
       - Change line 37: the Jira health check URL from `https://wandb.atlassian.net/rest/api/3/myself` to `https://coreweave.atlassian.net/rest/api/3/myself`
       - Change line 43: the success message from `ok "Jira (wandb.atlassian.net)"` to `ok "Jira (coreweave.atlassian.net)"`
       - The Confluence check on line 40 already uses coreweave.atlassian.net -- leave it unchanged
       - NOTE: After migration, Jira and Confluence are on the SAME instance (coreweave.atlassian.net). The check.sh script should now test both under a single instance. However, keeping separate checks is fine since they validate different services on the same host.

    3. In `.claude/rules/atlassian.md`:
       - Change line 5: `Jira: wandb.atlassian.net (W&B instance -- customer bugs, feature requests, escalations)` to `Jira: coreweave.atlassian.net (W&B project WB -- customer bugs, feature requests, escalations)`
       - Note: Confluence line already says coreweave.atlassian.net -- leave unchanged
       - Leave customfield_10083 and customfield_10084 references as-is -- these will be validated in Plan 03

    4. In `.claude/skills/atlassian-setup/SKILL.md`:
       - Replace all occurrences of `wandb.atlassian.net` with `coreweave.atlassian.net`
       - IMPORTANT: The "Wrong Jira instance" troubleshooting note (line 119 area) that says "The W&B Jira is at wandb.atlassian.net, not coreweave.atlassian.net" must be REMOVED or rewritten since Jira IS now at coreweave.atlassian.net. Rewrite to: "Both Jira and Confluence are at `coreweave.atlassian.net`. The W&B project key is `WB`."
  </action>
  <verify>
    <automated>cd /Users/astevenson/gitstuff/field-eng-skills && grep -c "wandb\.atlassian\.net" .claude/skills/jira/scripts/jira_client.py .claude/skills/credential-status/scripts/check.sh .claude/rules/atlassian.md .claude/skills/atlassian-setup/SKILL.md | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep "coreweave.atlassian.net" .claude/skills/jira/scripts/jira_client.py returns the JIRA_SERVER line
    - grep "wandb.atlassian.net" .claude/skills/jira/scripts/jira_client.py returns zero matches
    - grep "coreweave.atlassian.net/rest/api/3/myself" .claude/skills/credential-status/scripts/check.sh returns at least 1 match (Jira check)
    - grep "wandb.atlassian.net" .claude/skills/credential-status/scripts/check.sh returns zero matches
    - grep "wandb.atlassian.net" .claude/rules/atlassian.md returns zero matches
    - grep "wandb.atlassian.net" .claude/skills/atlassian-setup/SKILL.md returns zero matches
  </acceptance_criteria>
  <done>jira_client.py JIRA_SERVER points to coreweave.atlassian.net, check.sh validates against coreweave.atlassian.net, atlassian.md and atlassian-setup SKILL.md reference coreweave.atlassian.net exclusively</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md, README.md, and Jira SKILL.md</name>
  <files>
    CLAUDE.md
    README.md
    .claude/skills/jira/SKILL.md
  </files>
  <read_first>
    CLAUDE.md
    README.md
    .claude/skills/jira/SKILL.md
  </read_first>
  <action>
    Per D-01 and JIRA-04: Update all project-level documentation to reference the new instance.

    1. In `CLAUDE.md`:
       - Line 1 area: Change "Integrates with W&B Jira (wandb.atlassian.net)" to "Integrates with W&B Jira (coreweave.atlassian.net)"
       - Credential table: Change both ATLASSIAN_EMAIL and ATLASSIAN_TOKEN Instance column from `wandb.atlassian.net` to `coreweave.atlassian.net`
       - Change the Service column for these rows from "W&B Jira" to "W&B Jira" (keep as-is, it's the project name not the instance)
       - The Jira skill comment in the project structure (line 36 area) references wandb.atlassian.net -- update to coreweave.atlassian.net

    2. In `README.md`:
       - Line 16 area: Change "W&B Jira (wandb.atlassian.net)" references to "W&B Jira (coreweave.atlassian.net)"
       - Line 99 area: Jira skill description -- update instance reference
       - Line 180 area: Credential table -- update wandb.atlassian.net to coreweave.atlassian.net

    3. In `.claude/skills/jira/SKILL.md`:
       - Frontmatter: Change `service-url: https://wandb.atlassian.net` to `service-url: https://coreweave.atlassian.net`
       - Line 2/description: Change "W&B Jira instance (wandb.atlassian.net)" to "W&B Jira instance (coreweave.atlassian.net)"
       - Defaults table: Change Instance from `wandb.atlassian.net` to `coreweave.atlassian.net`
       - "Open issue in browser" example: Change `https://wandb.atlassian.net/browse/WB-123` to `https://coreweave.atlassian.net/browse/WB-123`
       - All other occurrences of wandb.atlassian.net in this file
  </action>
  <verify>
    <automated>cd /Users/astevenson/gitstuff/field-eng-skills && grep -c "wandb\.atlassian\.net" CLAUDE.md README.md .claude/skills/jira/SKILL.md | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - grep "wandb.atlassian.net" CLAUDE.md returns zero matches
    - grep "wandb.atlassian.net" README.md returns zero matches
    - grep "wandb.atlassian.net" .claude/skills/jira/SKILL.md returns zero matches
    - grep "coreweave.atlassian.net" CLAUDE.md returns matches for Jira credential rows AND Confluence credential rows
    - grep "service-url.*coreweave.atlassian.net" .claude/skills/jira/SKILL.md returns 1 match
  </acceptance_criteria>
  <done>CLAUDE.md credential table, README.md, and Jira SKILL.md all reference coreweave.atlassian.net exclusively -- no remaining wandb.atlassian.net references in project-level documentation</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "wandb\.atlassian\.net" .claude/skills/jira/ .claude/rules/atlassian.md .claude/skills/credential-status/ .claude/skills/atlassian-setup/ CLAUDE.md README.md` returns zero matches
2. `grep -c "coreweave\.atlassian\.net" .claude/skills/jira/scripts/jira_client.py` returns at least 1
</verification>

<success_criteria>
- The Jira API client (jira_client.py) JIRA_SERVER constant points to coreweave.atlassian.net
- The credential health check (check.sh) validates against coreweave.atlassian.net
- All 7 files in this plan contain zero references to wandb.atlassian.net
- All project documentation (CLAUDE.md, README.md) directs users/agents to coreweave.atlassian.net
</success_criteria>

<output>
After completion, create `.planning/phases/07-jira-instance-migration/07-01-SUMMARY.md`
</output>
