---
phase: 05-dashboard-v2-modular-folder-based-architecture
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .claude/skills/customer-snapshot/templates/panels/usage.js
autonomous: true
requirements: [DASH-07]

must_haves:
  truths:
    - "Usage panel renders 4 ECharts charts: seat utilization line, product adoption radar, Weave ingestion bar, tracked hours bar"
    - "Usage panel renders account health grid (DOM-based) for internal audience"
    - "Usage panel shows KPI summary row with seat %, Weave GB, and run count"
    - "All ECharts instances created via ChartHelpers.createChart() with wandb theme"
    - "Panel handles graceful degradation when usage data is unavailable"
  artifacts:
    - path: ".claude/skills/customer-snapshot/templates/panels/usage.js"
      provides: "Usage panel with 4 ECharts charts + account health grid extracted from v1"
      contains: "PanelRegistry.register"
      min_lines: 400
  key_links:
    - from: ".claude/skills/customer-snapshot/templates/panels/usage.js"
      to: "PanelRegistry"
      via: "PanelRegistry.register({ id: 'usage', ... })"
      pattern: "PanelRegistry\\.register"
    - from: ".claude/skills/customer-snapshot/templates/panels/usage.js"
      to: "ChartHelpers"
      via: "ChartHelpers.createChart() for all 4 ECharts instances"
      pattern: "ChartHelpers\\.createChart"
---

<objective>
Extract the Usage panel from the v1 monolith into a standalone panel JS file. This is a MEDIUM complexity extraction — 6 sub-chart render functions plus KPIs and account health grid. All ECharts instances must migrate from direct `echarts.init()` to `ChartHelpers.createChart()`.

Purpose: The Usage panel is the most chart-heavy panel with 4 ECharts instances. Migrating it validates that ChartHelpers handles multi-chart panels correctly.
Output: `panels/usage.js` (~500 lines) with 4 ECharts charts + DOM-based account health grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-RESEARCH.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-01-SUMMARY.md
@DASHBOARD-V2-SPEC.md

<interfaces>
<!-- V1 functions to extract (intelligence-dashboard.html) -->

renderUsagePanel() at lines 3641-3690:
  - Orchestrator: checks data.available, shows empty/content, renders KPIs, calls 5 sub-renderers
  - Sub-renderers:
    - renderSeatChart(usage) lines 3368-3430: ECharts line chart, seat history, contracted threshold line
    - renderRadarChart(usage) lines 3432-3502: ECharts radar chart, product areas, hidden if no data
    - renderWeaveChart(usage) lines 3504-3564: ECharts bar chart, Weave ingestion + unique users dual axis
    - renderHoursChart(usage) lines 3566-3611: ECharts bar chart, tracked hours weekly
    - renderAccountHealthGrid(data) lines 3613-3639: DOM-based grid, internal-only, renewal/ARR/health/churn
  - renderUsageKPIs(container, usage) lines 3337-3365: KPI row with seats, Weave, runs + trend arrows

<!-- V1 helper functions used by Usage panel -->
- isDark: boolean (from prefers-color-scheme check in v1 init)
- formatNumber(n): locale number formatting
- formatTrendHTML(change): renders up/down/flat arrow with percentage
- zoneClass(pct): returns CSS class for utilization percentage (green/amber/red zones)

<!-- INTELLIGENCE_DATA.usage schema -->
{
  available: true,
  period: { start: "2025-03-24", end: "2026-03-24" },
  seat_utilization: {
    contracted: 50, claimed: 42, active: 35, utilization_percent: 70.0,
    history: [{ week: "2025-04-07", contracted: 50, active: 28 }]
  },
  weave: {
    ingestion_gb: 156.3, limit_gb: 500.0, utilization_percent: 31.3,
    unique_users_last_90d: 12,
    history: [{ month: "2025-04", ingestion_gb: 8.2, unique_users: 5 }]
  },
  tracked_hours: {
    last_30d_hours: 1250.0, last_30d_run_count: 342,
    history: [{ week: "2025-04-07", tracked_hours: 180.5 }]
  },
  account_health: {  // internal-only
    renewal_date: "2026-09-15", arr: 250000.0, cs_tier: "Strategic",
    customer_health: "Green", churn_probability_3mo: 0.05,
    churn_probability_5mo: 0.08, subscription_plan: "Enterprise",
    deployment_type: "dedicated-cloud"
  },
  trends: {
    seat_utilization_change: 12.5, weave_ingestion_change: -3.2,
    tracked_hours_change: 8.7, run_count_change: 15.3
  },
  product_areas: [
    { area: "Experiments", total_events: 1800, unique_users: 25,
      monthly_events: [{month: "2025-04", count: 150, users: 12}] }
  ],
  power_users: [
    { username: "alice_ml", total_events: 5000, product_areas: ["Experiments"],
      last_activity: "2026-03-20" }
  ]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usage panel — KPIs, seat chart, radar chart, Weave chart, hours chart</name>
  <files>
    .claude/skills/customer-snapshot/templates/panels/usage.js
  </files>
  <read_first>
    .claude/skills/customer-snapshot/templates/intelligence-dashboard.html
    .claude/skills/customer-snapshot/templates/lib/panel-registry.js
    .claude/skills/customer-snapshot/templates/lib/chart-helpers.js
    .planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
  </read_first>
  <action>
Extract the Usage panel from v1 lines 3337-3690 into a standalone `panels/usage.js` file. Port all 6 sub-renderers. Migrate from `echarts.init(el, 'wandb')` to `ChartHelpers.createChart(el)`. The v1 monolith must remain UNCHANGED.

**File structure:** IIFE with PANEL_CSS, helper functions, sub-renderers, and PanelRegistry.register().

**Helper functions to include locally (since they exist in v1 global scope):**
```javascript
function isDark() {
  return window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
}

function formatNumber(n) {
  if (n === null || n === undefined) return '—';
  return n.toLocaleString();
}

function formatTrendHTML(change) {
  if (change === null || change === undefined) return '';
  const arrow = change > 0 ? '↑' : change < 0 ? '↓' : '→';
  const cls = change > 0 ? 'trend-up' : change < 0 ? 'trend-down' : 'trend-flat';
  return `<span class="${cls}">${arrow} ${Math.abs(change).toFixed(1)}%</span>`;
}

function zoneClass(pct) {
  if (pct >= 70) return 'zone-green';
  if (pct >= 40) return 'zone-amber';
  return 'zone-red';
}
```

**PANEL_CSS (~80 lines):**
- `.section-label` — JetBrains Mono 11px/600, uppercase, 1.5px letter-spacing, text-tertiary
- `.usage-kpi-row` — flex, gap clamp(24px, 4vw, 48px), flex-wrap, margin-bottom 24px
- `.usage-kpi-item` — flex column
- `.usage-kpi-value` — Outfit 14px/600, text-primary
- `.trend-up` — green color, JetBrains Mono 11px/400
- `.trend-down` — red color, JetBrains Mono 11px/400
- `.trend-flat` — text-tertiary, JetBrains Mono 11px/400
- `.zone-green` — green color
- `.zone-amber` — amber color
- `.zone-red` — red color
- `.usage-chart-section` — margin-bottom 32px
- `.usage-chart-section .chart-label` — JetBrains Mono 11px/600, uppercase, 1.5px letter-spacing, text-tertiary, margin-bottom 12px
- `.account-health-grid` — display grid, grid-template-columns repeat(auto-fill, minmax(200px, 1fr)), gap 16px, margin-top 24px
- `.health-card` — bg-elevated, 1px border-subtle, radius 6px, padding 16px
- `.health-card-label` — JetBrains Mono 11px/600, uppercase, text-tertiary
- `.health-card-value` — Outfit 14px/600, text-primary, margin-top 4px
- `.health-badge` — pill, JetBrains Mono 11px/600, uppercase, padding 2px 8px, radius 10px — Green: green/green-dim, Amber: amber/amber-dim, Red: red/red-dim

**Sub-renderers (port from v1, adapt to ChartHelpers):**

1. `renderKPIs(container, usage)` — Port v1 renderUsageKPIs (lines 3337-3365). Build HTML with seat utilization (active/contracted + %), Weave ingestion (GB used/limit + %), run count (last 30d). Each with trend arrow via formatTrendHTML(). Use zoneClass() for utilization coloring.

2. `renderSeatChart(chartEl, usage)` — Port v1 lines 3368-3430. Use `ChartHelpers.createChart(chartEl)` instead of `echarts.init(el, 'wandb')`. Line chart of weekly active users. Add threshold line at `contracted` level using `markLine`. Use `ChartHelpers.tooltipConfig()` for tooltip, `ChartHelpers.axisLabelConfig()` for axis labels. Color: blue for active line, accent for contracted threshold.

3. `renderRadarChart(chartEl, usage)` — Port v1 lines 3432-3502. Use `ChartHelpers.createChart(chartEl)`. Radar chart of product areas (total_events per area). Only render if `usage.product_areas` has data. Sort by total_events descending, take top 8. Indicator names = area names. Max values = 1.2x actual max. Color: accent for fill with 0.3 opacity.

4. `renderWeaveChart(chartEl, usage)` — Port v1 lines 3504-3564. Use `ChartHelpers.createChart(chartEl)`. Dual-axis bar chart: Weave ingestion GB on left axis (bars), unique users on right axis (line). Monthly data. Colors: blue for ingestion bars, accent for users line.

5. `renderHoursChart(chartEl, usage)` — Port v1 lines 3566-3611. Use `ChartHelpers.createChart(chartEl)`. Bar chart of weekly tracked hours. Color: blue for bars.

6. `renderAccountHealthGrid(container, usage)` — Port v1 lines 3613-3639. DOM-based grid (not ECharts). Cards for: renewal date, ARR (formatted as $XXXk), CS tier, customer health (colored badge), churn probability 3mo (percentage), churn probability 5mo, subscription plan, deployment type. Only show when `config.audience === 'internal'`.

**render(container, data, config):**
1. Check `data` (already resolved to `INTELLIGENCE_DATA.usage` by shell). If not available, show empty state with reason-specific message (same as v1 lines 3647-3658).
2. Build container HTML with sections for KPIs, each chart (with chart container divs at specific heights: seat 280px, radar 320px, weave 240px, hours 240px), and account health grid.
3. Call renderKPIs, then each chart sub-renderer with the appropriate container element and data.
4. Only call renderRadarChart if `data.product_areas && data.product_areas.length > 0` — otherwise hide that section.
5. Only call renderAccountHealthGrid if `config.audience === 'internal' && data.account_health`.
6. Collect all chart instances and return `{ charts }`.

**getHeadlineStats(data):**
```javascript
if (!data || !data.available) return [];
const stats = [];
if (data.seat_utilization) {
  stats.push({ label: 'Seat Utilization', value: Math.round(data.seat_utilization.utilization_percent) + '%', color: data.seat_utilization.utilization_percent >= 70 ? 'var(--green)' : data.seat_utilization.utilization_percent >= 40 ? 'var(--amber)' : 'var(--red)' });
}
if (data.weave) {
  stats.push({ label: 'Weave', value: data.weave.ingestion_gb.toFixed(0) + ' GB', color: 'var(--text-primary)' });
}
if (data.tracked_hours) {
  stats.push({ label: 'Runs (30d)', value: formatNumber(data.tracked_hours.last_30d_run_count), color: 'var(--text-primary)' });
}
return stats;
```

**getAttentionItems(data):**
- If seat utilization < 40%: `{ severity: 'medium', text: 'Low seat utilization ({pct}%)', action: { panel: 'usage' } }`
- If seat utilization trend < -10: `{ severity: 'medium', text: 'Seat utilization declining ({change}%)', action: { panel: 'usage' } }`
- If account_health.churn_probability_3mo > 0.2: `{ severity: 'high', text: 'Elevated churn risk ({pct}%)', action: { panel: 'usage' } }`

**Icon SVG** (trend line):
```
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline></svg>
```
  </action>
  <verify>
    <automated>test -f .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "PanelRegistry.register" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "id: 'usage'" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "ChartHelpers.createChart" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "renderSeatChart\|seatChart\|seat-chart" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "renderRadarChart\|radarChart\|radar-chart" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "renderWeaveChart\|weaveChart\|weave-chart" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "renderHoursChart\|hoursChart\|hours-chart" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "account-health\|accountHealth\|renderAccountHealth" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "getHeadlineStats" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "getAttentionItems" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -q "PANEL_CSS" .claude/skills/customer-snapshot/templates/panels/usage.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - usage.js is wrapped in IIFE `(function() { 'use strict'; ... })();`
    - usage.js calls `PanelRegistry.register({ id: 'usage', group: 'usage', ... })`
    - usage.js creates 4 ECharts instances via `ChartHelpers.createChart()` (seat, radar, weave, hours)
    - usage.js does NOT call `echarts.init()` directly — always through ChartHelpers
    - usage.js defines local helpers: isDark, formatNumber, formatTrendHTML, zoneClass
    - render() handles unavailable data with reason-specific messages
    - render() renders KPI row with seat utilization, Weave, and run count
    - render() conditionally renders radar chart (only when product_areas has data)
    - render() conditionally renders account health grid (only internal audience + data exists)
    - render() returns `{ charts: [...] }` with all ECharts instances
    - PANEL_CSS uses only `var(--token)` design tokens
    - getHeadlineStats returns seat utilization, Weave GB, and run count
    - getAttentionItems flags low utilization and elevated churn risk
    - v1 intelligence-dashboard.html is NOT modified
    - File is > 400 lines
  </acceptance_criteria>
  <done>Usage panel extracted with 4 ECharts charts (seat, radar, weave, hours), KPI summary, and account health grid. All charts use ChartHelpers. Internal-only sections gated on config.audience. Registers with PanelRegistry.</done>
</task>

</tasks>

<verification>
1. usage.js exists with 4 ECharts chart renderers and account health grid
2. All charts created via ChartHelpers.createChart() (no direct echarts.init)
3. KPI row renders with trend arrows
4. Account health grid only shows for internal audience
5. v1 intelligence-dashboard.html unchanged
</verification>

<success_criteria>
- Usage panel renders all 4 ECharts charts matching v1 visual quality
- Account health grid shows renewal, ARR, health, churn data for internal audience
- KPI summary row matches v1 output
- Panel composes correctly into v2 dashboard folder
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-04-SUMMARY.md`
</output>
