---
phase: 05-dashboard-v2-modular-folder-based-architecture
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .claude/skills/customer-snapshot/templates/panels/actions.js
  - .claude/skills/customer-snapshot/templates/panels/slack.js
autonomous: true
requirements: [DASH-06, DASH-08]

must_haves:
  truths:
    - "Actions panel renders Asana task table grouped by section with priority badges, overdue flags, and Jira cross-links"
    - "Slack panel renders sentiment score, channel summary, and hot threads list with Slack thread links"
    - "Both panels register with PanelRegistry and provide getHeadlineStats and getAttentionItems"
    - "Both panels handle graceful empty states when data is unavailable"
  artifacts:
    - path: ".claude/skills/customer-snapshot/templates/panels/actions.js"
      provides: "SE Actions panel extracted from v1 monolith renderActionsPanel()"
      contains: "PanelRegistry.register"
      min_lines: 200
    - path: ".claude/skills/customer-snapshot/templates/panels/slack.js"
      provides: "Slack sentiment panel extracted from v1 monolith renderSentimentPanel()"
      contains: "PanelRegistry.register"
      min_lines: 150
  key_links:
    - from: ".claude/skills/customer-snapshot/templates/panels/actions.js"
      to: "PanelRegistry"
      via: "PanelRegistry.register({ id: 'actions', ... })"
      pattern: "PanelRegistry\\.register"
    - from: ".claude/skills/customer-snapshot/templates/panels/slack.js"
      to: "PanelRegistry"
      via: "PanelRegistry.register({ id: 'slack', ... })"
      pattern: "PanelRegistry\\.register"
---

<objective>
Extract the Actions panel and Slack panel from the v1 monolith into standalone panel JS files. These are the two simplest extractions (LOW complexity per RESEARCH.md) — Actions is ~210 lines of render logic and Slack is ~90 lines. Combined into one plan since neither exceeds 15-minute execution alone.

Purpose: Prove monolith extraction works before tackling the complex panels (Usage, Issues). Both panels are self-contained with minimal shared dependencies.
Output: Two panel JS files ready to compose into the v2 dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-RESEARCH.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-01-SUMMARY.md
@DASHBOARD-V2-SPEC.md

<interfaces>
<!-- V1 functions to extract -->

From intelligence-dashboard.html:
- renderActionsPanel() at lines 1978-2236: Reads INTELLIGENCE_DATA.actions, filters by scope (my_tasks/team), sorts by overdue/priority/due_date, groups by section (In Progress, Waiting, To Do, Other), renders summary stats + task table with priority badges, overdue/stale flags, Jira cross-links, Asana links. Depends on: ACTIONS_PRIO_ORDER, ACTIONS_SECTION_COLORS constants, addAsanaBadgesToJiraIssues() interaction.
- renderSentimentPanel() at lines 2238-2325: Reads INTELLIGENCE_DATA.sentiment, renders sentiment score display (categorical + numeric), channel summary, hot threads list with Slack links, internal risk signals. Depends on: SENTIMENT_COLOURS, SENTIMENT_LABELS constants.

<!-- Panel contract from Plan 01 -->
PanelRegistry.register({
  id: string,
  group: string,
  label: string,
  icon: string (SVG),
  badgeKey: string,
  dataKey: string,
  render(container, data, config) { /* returns { charts: [] } */ },
  getHeadlineStats(data) { /* returns [{ label, value, color }] */ },
  getAttentionItems(data) { /* returns [{ severity, text, action }] */ }
});

<!-- INTELLIGENCE_DATA.actions schema -->
{
  available: true,
  source: "asana",
  current_user: { gid: "12345", name: "Allan Stevenson" },
  scope: "my_tasks",
  project_gid: "98765",
  project_url: "https://app.asana.com/0/98765",
  tasks: [{
    gid: "11111", name: "Follow up on SDK crash (WB-1234)",
    section: "In Progress", due_on: "2026-03-28",
    overdue: false, stale: false, stale_days: 2,
    priority: "P1",
    assignee: { gid: "12345", name: "Allan Stevenson" },
    linked_jira: "WB-1234",
    url: "https://app.asana.com/0/0/11111",
    modified_at: "2026-03-21T10:00:00Z"
  }],
  summary: { total: 8, in_progress: 3, waiting: 2, todo: 2, overdue: 1, stale: 1 }
}

<!-- INTELLIGENCE_DATA.sentiment schema -->
{
  available: true,
  channels_analyzed: ["#ext-gresearch"],
  period: { start: "2026-03-03", end: "2026-03-17" },
  overall: { score: "cautiously-negative", numeric: -0.3, summary: "..." },
  hot_threads: [{
    channel: "#ext-gresearch", thread_ts: "...",
    summary: "Frustration about...", sentiment: "negative",
    message_count: 12, participants: 4,
    url: "https://coreweave.slack.com/archives/C0XXX/p..."
  }],
  internal: { raw_analysis: "...", risk_signals: [...], recommended_actions: [...] }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actions panel extraction from v1 monolith</name>
  <files>
    .claude/skills/customer-snapshot/templates/panels/actions.js
  </files>
  <read_first>
    .claude/skills/customer-snapshot/templates/intelligence-dashboard.html
    .claude/skills/customer-snapshot/templates/lib/panel-registry.js
    .claude/skills/customer-snapshot/templates/lib/chart-helpers.js
    .planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
  </read_first>
  <action>
Extract `renderActionsPanel()` (v1 lines 1978-2236) into a standalone `panels/actions.js` file. The v1 monolith must remain UNCHANGED — v2 is additive.

**File structure:** IIFE with PANEL_CSS, helper constants, and PanelRegistry.register().

**Constants to copy from v1 into the panel file (self-contained, no shared state):**
```javascript
const ACTIONS_PRIO_ORDER = { 'P0': 0, 'P1': 1, 'P2': 2, 'P3': 3 };
const ACTIONS_SECTION_COLORS = {
  'In Progress': 'var(--blue)',
  'Waiting': 'var(--amber)',
  'To Do': 'var(--text-tertiary)',
  'Other': 'var(--text-tertiary)'
};
```

**PANEL_CSS (~60 lines):** Define scoped styles for:
- `.section-label` — JetBrains Mono 11px/600, uppercase, 1.5px letter-spacing, text-tertiary
- `.actions-summary` — flex row with gap 24px, margin-bottom 24px
- `.actions-summary-stat` — flex column, Outfit 28px/600 value + JetBrains Mono 11px/600 uppercase label
- `.actions-scope-toggle` — JetBrains Mono 11px/600, accent color, cursor pointer, underline on hover
- `.actions-group` — margin-bottom 24px
- `.actions-group-header` — flex between, JetBrains Mono 11px/600 uppercase text-tertiary section name + count badge
- `.task-table` — width 100%, border-collapse collapse
- `.task-table th` — JetBrains Mono 11px/600, uppercase, text-tertiary, padding 8px 12px, border-bottom 1px border
- `.task-table td` — Outfit 14px/400, padding 10px 12px, border-bottom 1px border-subtle
- `.task-table tr:hover` — bg-hover, transition 0.15s
- `.task-name` — Outfit 14px/400, max-width 400px, text-overflow ellipsis, overflow hidden, white-space nowrap
- `.task-overdue` — red color, JetBrains Mono 11px/600
- `.task-stale` — amber color, JetBrains Mono 11px/400
- `.priority-badge` — pill, JetBrains Mono 11px/600, uppercase, padding 2px 8px, radius 10px. P0/P1: red/red-dim, P2: amber/amber-dim, P3: gray/gray-dim
- `.jira-link` — JetBrains Mono 11px/400, blue, no underline, underline on hover
- `.asana-link` — JetBrains Mono 11px/400, accent, no underline, underline on hover
- `.actions-unavailable` — Outfit 14px/400, text-secondary, padding 24px
- `.actions-unavailable-hint` — JetBrains Mono 11px/400, text-tertiary, padding 0 24px

**render(container, data, config):**
Port the v1 renderActionsPanel() logic with these adaptations:
1. `data` parameter is already `INTELLIGENCE_DATA.actions` (resolved by shell via dataKey)
2. Check `data.available` — if false, render unavailable message with reason-specific text (same strings as v1 lines 1983-1993)
3. Use a local `_actionsScope` variable defaulting to `data.scope || 'my_tasks'`
4. Filter tasks by scope: if my_tasks and data.current_user, filter by assignee.gid match
5. Sort: overdue first, then priority (ACTIONS_PRIO_ORDER), then due_on ascending
6. Group into sections: In Progress, Waiting (both Waiting on Customer and Waiting on Eng), To Do, Other
7. Build summary stats row: total, in_progress, waiting, overdue (with red color), stale (with amber color)
8. Add scope toggle link: "View {otherScope}" clicking toggles scope and re-renders
9. For each non-empty group: render group header with section name + count, then task table
10. Task table columns: Priority (badge), Task (name with ellipsis), Due Date (formatted), Status (section name with color dot), Jira (link if linked_jira), Asana (link to task URL)
11. Overdue tasks: show "OVERDUE" label in red after due date
12. Stale tasks: show "stale {N}d" label in amber
13. Return `{ charts: [] }` (no ECharts in this panel)

**Re-render on scope toggle:**
The scope toggle needs to re-render the panel. Store a reference to the container and data, and on toggle click, call the render logic again with toggled scope. Use a closure: `let _actionsScope = data.scope || 'my_tasks';` then the toggle button calls a function that flips the scope and re-executes the rendering portion.

**getHeadlineStats(data):**
```javascript
if (!data || !data.available) return [];
return [
  { label: 'SE Actions', value: String(data.summary?.total || 0), color: 'var(--text-primary)' },
  { label: 'In Progress', value: String(data.summary?.in_progress || 0), color: 'var(--blue)' },
  { label: 'Overdue', value: String(data.summary?.overdue || 0), color: data.summary?.overdue > 0 ? 'var(--red)' : 'var(--text-tertiary)' }
];
```

**getAttentionItems(data):**
- If `data.summary?.overdue > 0`: `{ severity: 'high', text: '{N} overdue SE actions', action: { panel: 'actions' } }`
- If `data.summary?.stale > 0`: `{ severity: 'medium', text: '{N} stale actions (7+ days)', action: { panel: 'actions' } }`

**Icon SVG** (checkmark in box):
```
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 11l3 3L22 4"></path><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"></path></svg>
```
  </action>
  <verify>
    <automated>test -f .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "PanelRegistry.register" .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "id: 'actions'" .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "ACTIONS_PRIO_ORDER" .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "getHeadlineStats" .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "getAttentionItems" .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "PANEL_CSS" .claude/skills/customer-snapshot/templates/panels/actions.js && grep -q "overdue" .claude/skills/customer-snapshot/templates/panels/actions.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - actions.js is wrapped in IIFE `(function() { 'use strict'; ... })();`
    - actions.js calls `PanelRegistry.register({ id: 'actions', group: 'activity', ... })`
    - actions.js defines ACTIONS_PRIO_ORDER and ACTIONS_SECTION_COLORS as local constants
    - actions.js PANEL_CSS uses only `var(--token)` design tokens
    - render() handles `data.available === false` with reason-specific messages
    - render() sorts tasks: overdue first, then priority, then due_on
    - render() groups tasks into In Progress, Waiting, To Do, Other sections
    - render() renders scope toggle between my_tasks and team views
    - render() shows overdue flag in red and stale flag in amber
    - render() renders Jira links as `wandb.atlassian.net/browse/{key}` and Asana links to task URL
    - getHeadlineStats returns total, in_progress, overdue stats
    - getAttentionItems flags overdue and stale actions
    - v1 intelligence-dashboard.html is NOT modified
  </acceptance_criteria>
  <done>Actions panel extracted into standalone JS file with scope toggle, priority sorting, section grouping, overdue/stale flagging, and Jira/Asana cross-links. Registers with PanelRegistry.</done>
</task>

<task type="auto">
  <name>Task 2: Slack sentiment panel extraction from v1 monolith</name>
  <files>
    .claude/skills/customer-snapshot/templates/panels/slack.js
  </files>
  <read_first>
    .claude/skills/customer-snapshot/templates/intelligence-dashboard.html
    .claude/skills/customer-snapshot/templates/lib/panel-registry.js
    .planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
  </read_first>
  <action>
Extract `renderSentimentPanel()` (v1 lines 2238-2325) into a standalone `panels/slack.js` file. The v1 monolith must remain UNCHANGED.

**File structure:** IIFE with PANEL_CSS, sentiment constants, and PanelRegistry.register().

**Constants to copy from v1:**
```javascript
const SENTIMENT_LABELS = {
  'positive': 'Positive',
  'neutral': 'Neutral',
  'cautiously-negative': 'Cautiously Negative',
  'negative': 'Negative',
  'critical': 'Critical'
};
const SENTIMENT_COLOURS = {
  'positive': 'var(--green)',
  'neutral': 'var(--text-secondary)',
  'cautiously-negative': 'var(--amber)',
  'negative': 'var(--orange)',
  'critical': 'var(--red)'
};
```

**PANEL_CSS (~50 lines):** Define scoped styles for:
- `.section-label` — JetBrains Mono 11px/600, uppercase, 1.5px letter-spacing, text-tertiary
- `.sentiment-header` — flex, align-items center, gap 12px, margin-bottom 16px
- `.sentiment-score-display` — Outfit 28px/600, color set by JS per sentiment category
- `.sentiment-numeric` — JetBrains Mono 11px/400, text-tertiary
- `.sentiment-summary` — Outfit 14px/400, text-secondary, margin-bottom 16px, line-height 1.55
- `.sentiment-channels` — JetBrains Mono 11px/400, text-tertiary, margin-bottom 24px
- `.hot-threads-list` — flex column, gap 12px
- `.hot-thread` — bg-elevated, 1px border-subtle, radius 6px, padding 16px
- `.hot-thread-channel` — JetBrains Mono 11px/600, accent color
- `.hot-thread-summary` — Outfit 14px/400, text-primary, display block, margin-top 4px
- `.hot-thread-meta` — flex, gap 12px, margin-top 8px, align-items center
- `.hot-thread-stat` — JetBrains Mono 11px/400, text-tertiary
- `.hot-thread-sentiment` — JetBrains Mono 11px/600, uppercase — colored by sentiment via inline style
- `.hot-thread-link` — JetBrains Mono 11px/400, blue, no underline, underline on hover
- `.sentiment-unavailable` — Outfit 14px/400, text-secondary, padding 24px
- `.internal-section` — margin-top 24px, border-top 1px border-subtle, padding-top 24px
- `.risk-signal` — Outfit 14px/400, text-secondary, padding 4px 0
- `.risk-signal::before` — content "⚠ " (warning emoji prefix)
- `.recommended-action` — Outfit 14px/400, text-secondary, padding 4px 0

**render(container, data, config):**
Port the v1 renderSentimentPanel() logic with these adaptations:
1. `data` parameter is already `INTELLIGENCE_DATA.sentiment` (resolved by shell via dataKey)
2. Check: if `!data || !data.available`, render unavailable message. If data is null: "Not configured -- add Slack channels to templates/customers.yaml". If data.available is false: "Slack data unavailable for this period."
3. Render sentiment header: score display (colored by SENTIMENT_COLOURS[data.overall.score]), numeric score display
4. Render summary text from `data.overall.summary`
5. Render channels analyzed and period
6. Render hot threads (up to 5): each in a card with channel name, summary, meta row (message_count, participants, sentiment badge colored by SENTIMENT_COLOURS), "View in Slack" link if URL present
7. If `config.audience === 'internal'` and `data.internal`: render internal section with risk signals and recommended actions
8. Return `{ charts: [] }` (no ECharts)

**getHeadlineStats(data):**
```javascript
if (!data || !data.available) return [];
const scoreColor = SENTIMENT_COLOURS[data.overall?.score] || 'var(--text-secondary)';
return [
  { label: 'Sentiment', value: SENTIMENT_LABELS[data.overall?.score] || 'Unknown', color: scoreColor },
  { label: 'Hot Threads', value: String(data.hot_threads?.length || 0), color: (data.hot_threads?.length || 0) > 0 ? 'var(--orange)' : 'var(--text-tertiary)' }
];
```

**getAttentionItems(data):**
- If sentiment is 'negative' or 'critical': `{ severity: 'high', text: 'Slack sentiment is {label}', action: { panel: 'slack' } }`
- If hot_threads.length > 2: `{ severity: 'medium', text: '{N} hot threads in Slack', action: { panel: 'slack' } }`
- If `data.internal?.risk_signals?.length > 0`: `{ severity: 'high', text: '{N} risk signals detected', action: { panel: 'slack' } }`

**Icon SVG** (Slack-style message icon — same as v2 prototype for consistency):
Use the Slack icon SVG from dashboard-v2.html line 1024 (simplified paths).
  </action>
  <verify>
    <automated>test -f .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "PanelRegistry.register" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "id: 'slack'" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "SENTIMENT_LABELS" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "SENTIMENT_COLOURS" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "getHeadlineStats" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "getAttentionItems" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "hot-thread" .claude/skills/customer-snapshot/templates/panels/slack.js && grep -q "PANEL_CSS" .claude/skills/customer-snapshot/templates/panels/slack.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - slack.js is wrapped in IIFE `(function() { 'use strict'; ... })();`
    - slack.js calls `PanelRegistry.register({ id: 'slack', group: 'activity', ... })`
    - slack.js defines SENTIMENT_LABELS and SENTIMENT_COLOURS as local constants
    - slack.js PANEL_CSS uses only `var(--token)` design tokens
    - render() handles null sentiment (not configured) and available=false (unavailable)
    - render() displays sentiment score colored by category
    - render() renders up to 5 hot threads with channel, summary, meta, and Slack link
    - render() renders internal risk signals and recommended actions when audience is internal
    - getHeadlineStats returns sentiment label and hot thread count
    - getAttentionItems flags negative/critical sentiment and risk signals
    - v1 intelligence-dashboard.html is NOT modified
  </acceptance_criteria>
  <done>Slack sentiment panel extracted into standalone JS file with sentiment display, hot threads, internal risk signals, and Slack links. Registers with PanelRegistry. Handles graceful degradation when Slack is not configured.</done>
</task>

</tasks>

<verification>
1. Both panel JS files exist in templates/panels/
2. Both register with PanelRegistry with correct group assignments
3. Both handle unavailable data gracefully
4. Both provide getHeadlineStats and getAttentionItems for Overview aggregation
5. v1 intelligence-dashboard.html is identical to before (no modifications)
</verification>

<success_criteria>
- Actions panel renders task table with scope toggle, priority sorting, section grouping, and cross-links
- Slack panel renders sentiment score, hot threads, and internal sections
- Both panels compose into v2 dashboard folder via compose.py
- v1 monolith remains unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-03-SUMMARY.md`
</output>
