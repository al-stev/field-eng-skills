---
phase: 05-dashboard-v2-modular-folder-based-architecture
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .claude/skills/customer-snapshot/templates/panels/support-tickets.js
autonomous: false
requirements: [DASH-05]

must_haves:
  truths:
    - "Support panel renders 5 ECharts visualizations: volume trend bar, concern treemap, age scatter, submitter stacked bars, submitter-concern heatmap"
    - "Support panel headline stats strip shows total tickets, active count, resolved %, and avg/month"
    - "Scatter plot points link to Jira tickets via click handler opening wandb.atlassian.net/browse/{jira_id}"
    - "Panel registers with PanelRegistry including getHeadlineStats and getAttentionItems"
  artifacts:
    - path: ".claude/skills/customer-snapshot/templates/panels/support-tickets.js"
      provides: "Support Tickets panel with 5 prototype-quality visualizations"
      contains: "PanelRegistry.register"
      min_lines: 400
  key_links:
    - from: ".claude/skills/customer-snapshot/templates/panels/support-tickets.js"
      to: "PanelRegistry"
      via: "PanelRegistry.register({ id: 'support', ... })"
      pattern: "PanelRegistry\\.register"
    - from: ".claude/skills/customer-snapshot/templates/panels/support-tickets.js"
      to: "ChartHelpers"
      via: "ChartHelpers.createChart() for all 5 ECharts instances"
      pattern: "ChartHelpers\\.createChart"
---

<objective>
Build the Support Tickets panel at prototype quality with 5 ECharts visualizations: monthly volume trend, concern treemap, active ticket age scatter, top submitter stacked bars with sparklines, and submitter-concern heatmap. This is the showcase panel — built from committed prototypes, not extracted from v1.

Purpose: Validates the panel contract (PanelRegistry.register) with a fresh panel before attempting v1 extraction. Proves the shell + registry + chart-helpers infrastructure works end-to-end.
Output: `panels/support-tickets.js` (~700-800 lines) registering a fully functional panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-RESEARCH.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-01-SUMMARY.md
@DASHBOARD-V2-SPEC.md

<interfaces>
<!-- Panel contract from panel-registry.js (created in Plan 01) -->
PanelRegistry.register({
  id: 'support',
  group: 'intelligence',
  label: 'Support',
  icon: '<svg ...>...</svg>',
  badgeKey: 'usage.support_tickets.total',
  dataKey: 'usage.support_tickets',
  render(container, data, config) { /* returns { charts: [] } */ },
  getHeadlineStats(data) { /* returns [{ label, value, color }] */ },
  getAttentionItems(data) { /* returns [{ severity, text, action }] */ }
});

<!-- ChartHelpers API (created in Plan 01) -->
ChartHelpers.createChart(container)  // returns echarts instance with 'wandb' theme
ChartHelpers.tooltipConfig()         // returns tooltip styling object
ChartHelpers.axisLabelConfig()       // returns axis label styling object
ChartHelpers.gridLine()              // returns splitLine styling object
ChartHelpers.getColor(tokenName)     // returns CSS custom property value
ChartHelpers.resizeAll()             // resizes all tracked instances
ChartHelpers.daysBetween(dateStr)    // days between dateStr and now
ChartHelpers.formatMonth(ym)         // "2025-03" -> "Mar 25"

<!-- Data contract (from INTELLIGENCE_DATA.usage.support_tickets) -->
{
  total: 30,
  by_status: { closed: 21, hold: 8, solved: 1 },
  by_priority: { high: 28, urgent: 2 },
  escalated_to_jira: 0,
  csat: { offered: 18, unoffered: 10 },
  top_concerns: [{ concern: "workspace", count: 6 }, ...],
  monthly_volume: [{ month: "2025-03", count: 1 }, ...],
  recent_tickets: [{ id: 113975, subject: "...", status: "hold", priority: "high",
    created_at: "2026-03-17", updated_at: "...", concern: "workspace",
    channel: "api", jira_id: "WB-32236" }],
  top_submitters: [{ name: "Nikitas Rontsis", count: 19,
    concerns: [{ concern: "workspace", count: 4 }, ...] }]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Support Tickets panel — stats strip, volume trend, concern treemap, and age scatter</name>
  <files>
    .claude/skills/customer-snapshot/templates/panels/support-tickets.js
  </files>
  <read_first>
    .claude/skills/customer-snapshot/templates/dashboard-v2.html
    .claude/skills/customer-snapshot/prototypes/support-tickets/2-monthly-volume-trend.html
    .claude/skills/customer-snapshot/prototypes/support-tickets/3-concern-treemap.html
    .claude/skills/customer-snapshot/prototypes/support-tickets/4-ticket-age-scatter.html
    .claude/skills/customer-snapshot/prototypes/support-tickets/5-submitter-activity.html
    .claude/skills/customer-snapshot/templates/lib/panel-registry.js
    .claude/skills/customer-snapshot/templates/lib/chart-helpers.js
    .planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-UI-SPEC.md
  </read_first>
  <action>
Create `panels/support-tickets.js` as a complete panel file. The file MUST be an IIFE calling `PanelRegistry.register()` at the end. All CSS uses `var(--token)` design tokens — NEVER hardcode hex colors. Port chart LOGIC from prototypes but NOT their design tokens (prototypes 3 and 4 use different color systems per RESEARCH.md Prototype Fidelity Notes).

**File structure:**
```javascript
(function() {
  'use strict';

  // --- CSS ---
  const PANEL_CSS = `...`;

  // --- HELPERS ---
  function humanizeConcern(raw) { ... }
  function ageBadge(days) { ... }
  function priorityBadge(priority) { ... }

  // --- REGISTRATION ---
  PanelRegistry.register({ id: 'support', ... });
})();
```

**PANEL_CSS** — All styles that the shell will auto-scope with `#panel-support` prefix:
- `.stats-strip` — 4-col grid, gap 16px, margin-bottom 32px. At 700px breakpoint: 2-col.
- `.stat-card` — bg-elevated, 1px border-subtle, radius 6px, padding 20px
- `.stat-value` — Outfit 28px/600 (per UI-SPEC stat value tier)
- `.stat-label` — JetBrains Mono 11px/600, uppercase, 1.5px letter-spacing, text-tertiary
- `.stat-sub` — JetBrains Mono 11px/400, text-tertiary
- `.section-label` — JetBrains Mono 11px/600, uppercase, 1.5px letter-spacing, text-tertiary, margin-bottom 16px
- `.two-col` — 2-col grid, gap 24px. At 700px: 1-col.
- `.panel-card` — bg-elevated, 1px border-subtle, radius 6px, padding 24px
- `.ticket-table` — width 100%, border-collapse collapse
- `.ticket-table th` — JetBrains Mono 11px/600, uppercase, text-tertiary, text-align left, padding 8px 12px, border-bottom 1px border
- `.ticket-table td` — Outfit 14px/400, padding 10px 12px, border-bottom 1px border-subtle, vertical-align middle
- `.ticket-table tr:hover` — bg-hover, transition 0.15s
- `.age-badge` — display inline-block, padding 2px 8px, radius 10px, JetBrains Mono 11px/400
- `.age-badge.green` — green color, green-dim bg, green-border border
- `.age-badge.amber` — amber color, amber-dim bg, amber-border border
- `.age-badge.red` — red color, red-dim bg, red-border border
- `.priority-badge` — similar pill styling with color by severity
- `.jira-link` — JetBrains Mono 11px/400, blue color, no underline, underline on hover
- `.concern-label` — JetBrains Mono 11px/400, text-tertiary, bg-surface, padding 2px 8px, radius 10px
- `.scatter-detail-toggle` — cursor pointer, JetBrains Mono 11px/600, text-secondary, margin-top 12px
- `.scatter-detail-table` — initially display:none, shown on toggle

**Helpers:**
- `humanizeConcern(raw)` — Map snake_case concern names to display names. Map: workspace->Workspace, crashed_run->Crashed Runs, app__runs_table->Runs Table, charts->Charts, performance->Performance, client__authentication->Authentication, reports->Reports, admin__service_accounts->Service Accounts, connectivity->Connectivity, resuming->Resuming, app__other->App (Other), app__login->Login, client__public_api->Public API, cloud_services->Cloud Services. Fallback: replace `__` with ` / `, `_` with ` `, title case.
- `ageBadge(days)` — returns HTML string `<span class="age-badge {color}">{days}d</span>`. Green: <30d, amber: 30-90d, red: >90d.
- `priorityBadge(priority)` — returns HTML string for priority pill. urgent->red, high->amber, normal->blue, low->gray.

**render(container, data, config):**

The `data` parameter is already resolved to `INTELLIGENCE_DATA.usage.support_tickets` by the shell (via dataKey).

Build DOM via `container.innerHTML = ...` with these sections:

1. **Stats strip** (4 stat cards):
   - Total tickets (12mo): `data.total`, color text-primary
   - Active: count of `data.recent_tickets.filter(t => t.status !== 'closed' && t.status !== 'solved')`, color amber
   - Resolved %: `Math.round(((data.by_status.closed || 0) + (data.by_status.solved || 0)) / data.total * 100)`, color green
   - Avg/month: `(data.total / Math.max(data.monthly_volume.length, 1)).toFixed(1)`, color text-primary

2. **Two-col: Volume Trend + Concern Treemap**

   **Monthly Volume Trend** (port from prototype 2 + v2 prototype lines 1360-1441):
   - ECharts bar chart via `ChartHelpers.createChart(container.querySelector('#chart-volume'))`
   - X-axis: months formatted via `ChartHelpers.formatMonth()`
   - Y-axis: ticket count, minInterval 1
   - Bar color: `ChartHelpers.getColor('blue')` for normal months, `ChartHelpers.getColor('amber')` for peak month
   - markPoint on max value with pin symbol, accent color
   - Tooltip via `ChartHelpers.tooltipConfig()`
   - Grid: left 48, right 24, top 24, bottom 32

   **Concern Treemap** (port from prototype 3 — chart logic only, NOT design tokens):
   - ECharts treemap via `ChartHelpers.createChart(container.querySelector('#chart-concerns'))`
   - Data: `data.top_concerns.map(c => ({ name: humanizeConcern(c.concern), value: c.count }))`
   - Color mapping: use treemap's built-in `colorMappingBy: 'value'` with `visualMap` continuous from `ChartHelpers.getColor('amber')` (low) to `ChartHelpers.getColor('red')` (high). If all counts are equal, use single `ChartHelpers.getColor('accent')`.
   - Label: Outfit 14px inside each cell with count
   - Breadcrumb: hidden (`show: false`)
   - Tooltip: show concern name and count with percentage of total

3. **Full-width: Active Tickets Scatter** (port from prototype 4 — chart logic only):
   - ECharts scatter via `ChartHelpers.createChart(container.querySelector('#chart-scatter'))`
   - Only active tickets: `data.recent_tickets.filter(t => t.status !== 'closed' && t.status !== 'solved')`
   - X-axis: created_at dates
   - Y-axis: age in days (computed via `ChartHelpers.daysBetween(t.created_at)`)
   - Color zones via `markArea`: green zone (0-30d), amber zone (30-90d), red zone (90+d) — using dim variants of each color with 0.08 opacity
   - Symbol size: 12 for high, 18 for urgent
   - Symbol color: amber for high, red for urgent
   - Click handler: `chart.on('click', params => { if (params.data.jiraId) window.open('https://wandb.atlassian.net/browse/' + params.data.jiraId, '_blank'); })`
   - Tooltip: show subject, priority, age, Jira ID

   Below the scatter, add a collapsible detail table:
   - Toggle link "Show detail table" / "Hide detail table"
   - Table with columns: Jira ID (link), Subject (ellipsis at 300px), Priority (badge), Age (badge), Status, Concern (label)
   - Sorted by age descending

4. **Two-col: Top Submitters + Submitter-Concern Heatmap** (port from prototype 5):

   **Top Submitters Stacked Bars:**
   - ECharts horizontal stacked bar via `ChartHelpers.createChart()`
   - Y-axis: submitter names (reversed for top-first display)
   - Each bar stacked by concern type, using a color palette cycling through: accent, blue, green, amber, red, orange
   - Tooltip: submitter name, per-concern breakdown
   - Label: total count on right

   **Submitter-Concern Heatmap:**
   - ECharts heatmap via `ChartHelpers.createChart()`
   - X-axis: unique concern types (humanized)
   - Y-axis: submitter names
   - Cell value: count of tickets for that submitter + concern
   - visualMap continuous: low=`ChartHelpers.getColor('blue-dim')` to high=`ChartHelpers.getColor('blue')`
   - Tooltip: submitter, concern, count

Collect all chart instances into an array and return `{ charts }`.

**getHeadlineStats(data):**
```javascript
return [
  { label: 'Tickets (12mo)', value: String(data.total), color: 'var(--text-primary)' },
  { label: 'Active', value: String(activeCount), color: 'var(--amber)' },
  { label: 'Resolved', value: resolvedPct + '%', color: 'var(--green)' }
];
```

**getAttentionItems(data):**
- If any tickets have age > 90 days and status !== 'closed'/'solved': `{ severity: 'high', text: '{N} tickets stale 90+ days', action: { panel: 'support', filter: 'stale' } }`
- If any tickets have priority 'urgent': `{ severity: 'high', text: '{N} urgent tickets open', action: { panel: 'support', filter: 'urgent' } }`
- If total > 0 and (closed+solved)/total < 0.5: `{ severity: 'medium', text: 'Low resolution rate ({pct}%)', action: { panel: 'support' } }`

**Icon SVG** (for sidebar — simple headset/support icon):
```
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
```
  </action>
  <verify>
    <automated>test -f .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "PanelRegistry.register" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "id: 'support'" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "getHeadlineStats" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "getAttentionItems" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "ChartHelpers.createChart" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "chart-volume" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "chart-concerns" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "chart-scatter" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "chart-submitters" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "chart-heatmap" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "humanizeConcern" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && grep -q "PANEL_CSS" .claude/skills/customer-snapshot/templates/panels/support-tickets.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - support-tickets.js is wrapped in IIFE `(function() { 'use strict'; ... })();`
    - support-tickets.js calls `PanelRegistry.register({ id: 'support', ... })`
    - support-tickets.js defines PANEL_CSS constant with all styles using `var(--token)` tokens
    - support-tickets.js creates 5 ECharts instances via `ChartHelpers.createChart()`
    - support-tickets.js contains chart containers: chart-volume, chart-concerns, chart-scatter, chart-submitters, chart-heatmap
    - render() returns `{ charts: [...] }` with all 5 ECharts instances
    - Scatter chart has click handler opening `wandb.atlassian.net/browse/{jira_id}`
    - getHeadlineStats returns array with total, active, resolved % stats
    - getAttentionItems flags tickets stale 90+ days and urgent priority
    - No hardcoded hex colors — all colors via `var(--token)` in CSS or `ChartHelpers.getColor()` in JS
    - No `type="module"`, `import`, or `export` syntax
    - humanizeConcern maps snake_case to display names with fallback
    - File is > 400 lines (prototype-quality visualizations require substantial code)
  </acceptance_criteria>
  <done>Support Tickets panel renders 5 ECharts visualizations at prototype quality, registers with PanelRegistry, provides headline stats and attention items for Overview aggregation. All CSS uses design tokens. Jira links clickable in scatter plot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of Support panel in v2 shell</name>
  <files>.claude/skills/customer-snapshot/templates/panels/support-tickets.js</files>
  <action>Human verifies: Support Tickets panel with 5 ECharts visualizations (volume trend, concern treemap, age scatter with Jira links, submitter stacked bars, submitter-concern heatmap) within the v2 dashboard shell</action>
  <how-to-verify>
    1. Run compose.py to generate a test dashboard with the Isomorphic Labs data from dashboard-v2.html:
       ```
       cd .claude/skills/customer-snapshot/templates
       python3 compose.py --customer "Isomorphic Labs" --data <path-to-test-data.json> --output /tmp/test-dashboard
       ```
       (The executor should create a test data JSON from the INTELLIGENCE_DATA in dashboard-v2.html)
    2. Open `/tmp/test-dashboard/index.html` in Chrome
    3. Verify sidebar shows Support with badge count "30"
    4. Click Support panel — verify:
       - Stats strip: 30 total, 8 active, 73% resolved, ~2.5 avg/month
       - Monthly volume bar chart with peak annotation on Nov 2025
       - Concern treemap with "Workspace" as largest tile
       - Active tickets scatter plot with color zones (green/amber/red)
       - Click a scatter point — should open Jira link in new tab
       - Top submitters stacked bars with Nikitas Rontsis at top
       - Submitter-concern heatmap grid
    5. Verify dark mode renders correctly
    6. Resize browser — charts should resize responsively
    7. At 700px width — stats strip should become 2-col, two-col sections become single-col
  </how-to-verify>
  <verify>User opens dashboard in Chrome and confirms 5 charts render, Jira links work, responsive layout works</verify>
  <done>User has approved Support panel visual quality and functionality</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. support-tickets.js exists and registers with PanelRegistry
2. All 5 chart containers present in rendered DOM
3. Charts render with real Isomorphic Labs data
4. Jira links open in new tab from scatter plot
5. Responsive layout works at 700px breakpoint
6. Design tokens used throughout (no hardcoded colors)
</verification>

<success_criteria>
- Support panel renders 5 ECharts visualizations matching prototype quality
- Stats strip shows accurate computed values (total, active, resolved %, avg/month)
- Scatter plot enables click-to-Jira navigation
- Panel contract validated: register, render, getHeadlineStats, getAttentionItems all work
- Visual verification by user confirms prototype-quality output
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-02-SUMMARY.md`
</output>
