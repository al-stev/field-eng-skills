---
phase: 03-medium-confidence-pages
task: iteration
total_tasks: 3 pages need design fixes
status: in_progress
last_updated: 2026-03-27T15:39:54.518Z
---

<current_state>
Phase 3 executed and verified. All 3 pages (cohort-analysis, team-detection, risk-scoring) generate with real BQ data. User has reviewed all 3 pages and given specific design feedback. Bug fixes committed. Dedicated cloud data model documented in SKILL.md files. Now need to iterate on page quality based on user feedback.
</current_state>

<completed_work>

- Phase 3 planned (4 plans, 3 waves), executed, verified, marked complete in ROADMAP
- All 3 pages generate successfully for real customers
- Bug: cohort_retention_query missing date filter on JOIN (54GB->34GB scan) - FIXED
- Bug: team_detection_query missing product_area column - FIXED (added PRODUCT_AREA_CASE mapping)
- Bug: team_detection bytes_billed limit too low (50GB->100GB) - FIXED
- Bug: null date range display ("null -- null") in base-template.html - FIXED
- Deep investigation into dedicated cloud data model - entity/team names are intentionally hashed in BQ telemetry
- Documented dedicated cloud data model in bigquery/SKILL.md and deep-analytics/SKILL.md
- Scrubbed customer-specific details from all documentation
</completed_work>

<remaining_work>

**Cohort Analysis page:**
- Retention heatmap needs better explanation for the SE - what does each cell mean practically?
- AI narrative is generating useless insights (e.g., "users who start with Experiments show strongest retention" - Experiments is the core product, everyone starts there)
- Narrative needs to identify actually meaningful patterns, not obvious ones

**Team Detection page:**
- Fundamentally broken for dedicated cloud - currently uses org_name grouping (shows 2 orgs) instead of the 20+ real entity/team groupings
- Needs query rewrite to use fct_local_runs entity groupings + intermediate_local_users for member names
- gneprop_runs-org is likely a Weave academic trial, not meaningful team data
- For SaaS customers it may work better but hasn't been tested

**Risk Scoring page:**
- Largely unexciting - raises questions but doesn't answer them
- Support ticket backlog and velocity are the interesting parts - worth expanding with more detail
- Needs to be more actionable, not just show a number
- Gauge and radar are present but the composite score isn't clearly meaningful to the SE

**Cross-cutting:**
- Identity resolution (identity_resolution_cte) needs updating to use intermediate_local_users for dedicated cloud
- All pages need to detect deployment type and branch query strategy accordingly
</remaining_work>

<decisions_made>

- Dedicated cloud entity/team names are intentionally hashed in BQ - this is a privacy feature, not a bug
- Team structure IS recoverable via fct_local_runs grouped by hashed entity_name + JOIN intermediate_local_users
- Real team names can only be found by querying the dedicated cloud instance directly (not BQ)
- intermediate_local_users is the correct table for dedicated cloud identity resolution (not dim_users)
- team_detection_query bytes_billed raised to 100GB because product_area CASE mapping expands scan size
</decisions_made>

<blockers>
- Team detection for dedicated cloud requires fundamental query rewrite (fct_local_runs approach)
- No way to get real entity/team names from BQ for dedicated cloud - must show anonymous groupings with member names
</blockers>

<context>
User is an SE who tested all 3 pages against Isomorphic Labs (dedicated cloud customer). The team detection investigation consumed most of the session - we searched ~30 BQ tables looking for the "Pythia" team name before discovering entity names are hashed. The user wants pages that are actually useful for customer conversations, not just technically correct. The existing pages work mechanically but the insights and data presentation need significant improvement to be valuable to an SE.

Test customers used: GResearch (server + small cloud - not ideal), Isomorphic Labs (dedicated cloud - entity names hashed). For visual iteration, a SaaS customer would give cleaner data. Phase 2 pages (user-journey, engagement-decay, feature-velocity, sdk-versions) are running in a parallel session.
</context>

<next_action>
Start with: Fix team detection query to use fct_local_runs + intermediate_local_users for dedicated cloud, then regenerate all 3 pages and iterate on design feedback. Consider testing against a SaaS customer for cleaner visual iteration.
</next_action>
