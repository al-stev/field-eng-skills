---
phase: 06-dashboard-v2-ux-polish-data-provenance-split-charts-navigation-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/bigquery/scripts/queries.py
  - .claude/skills/bigquery/scripts/usage.py
  - .claude/skills/customer-snapshot/templates/panels/usage.js
autonomous: true
requirements: [UX-02, UX-05, UX-06]

must_haves:
  truths:
    - "Product adoption shows two separate radar charts (Events and Users) that are independently readable"
    - "Sweeps data distinguishes between created and viewed in product adoption"
    - "Seat utilization chart renders without top label clipping"
    - "Tracked hours chart renders without bottom x-axis date label clipping"
  artifacts:
    - path: ".claude/skills/bigquery/scripts/queries.py"
      provides: "PRODUCT_AREA_CASE with Sweeps Created and Sweeps Viewed as separate areas"
      contains: "Sweeps Created"
    - path: ".claude/skills/bigquery/scripts/usage.py"
      provides: "Unchanged builder — product_areas already aggregates by area name from CASE"
    - path: ".claude/skills/customer-snapshot/templates/panels/usage.js"
      provides: "Two radar charts side by side + fixed chart margins"
      min_lines: 850
  key_links:
    - from: ".claude/skills/bigquery/scripts/queries.py"
      to: ".claude/skills/bigquery/scripts/usage.py"
      via: "product_areas_query() called by usage.py"
      pattern: "product_areas_query"
    - from: ".claude/skills/customer-snapshot/templates/panels/usage.js"
      to: "INTELLIGENCE_DATA.usage.product_areas"
      via: "renderRadarChart reads product_areas array"
      pattern: "data\\.product_areas"
---

<objective>
Fix chart readability and data accuracy in the Usage panel: split the overlaid Events+Users radar into two side-by-side charts, separate sweeps created vs viewed in the BQ query, and fix chart clipping on seat utilization and tracked hours.

Purpose: User testing found the overlaid radar unreadable, sweeps data undifferentiated, and chart labels clipped despite two prior fix attempts.
Output: Updated queries.py (sweeps split), updated usage.js (dual radar + margin fixes)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-v2-modular-folder-based-architecture/05-04-SUMMARY.md
@.claude/skills/bigquery/scripts/queries.py
@.claude/skills/bigquery/scripts/usage.py
@.claude/skills/customer-snapshot/templates/panels/usage.js

<interfaces>
<!-- Key types and contracts the executor needs -->

From queries.py (lines 37-49) — PRODUCT_AREA_CASE mapping:
```python
PRODUCT_AREA_CASE = """CASE
        WHEN event IN ('sweep_created', 'sweep_viewed') THEN 'Sweeps'
        ...
    END"""
```
This CASE statement is used by product_areas_query() (line 230). Sweeps events are
currently grouped together. They need to be split into 'Sweeps Created' and 'Sweeps Viewed'.

From usage.js — renderRadarChart (lines 328-417):
- Takes chartEl + data, creates single radar with overlaid Events + Users series
- Sorts product_areas by total_events, takes top 8
- Uses ChartHelpers.createChart(chartEl) for ECharts init

From usage.js — render function layout (lines 716-724):
- Radar section sits inside .usage-two-col grid alongside Weave chart
- Container: `<div id="usage-radar-chart" style="width:100%;height:380px;">`

From usage.js — seat chart grid config (line 270):
```javascript
grid: { left: 48, right: 80, top: 36, bottom: 32 }
```

From usage.js — hours chart grid config (line 531):
```javascript
grid: { left: 72, right: 24, top: 24, bottom: 64 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split sweeps in PRODUCT_AREA_CASE and fix chart clipping</name>
  <read_first>
    .claude/skills/bigquery/scripts/queries.py
    .claude/skills/customer-snapshot/templates/panels/usage.js
  </read_first>
  <files>
    .claude/skills/bigquery/scripts/queries.py
    .claude/skills/customer-snapshot/templates/panels/usage.js
  </files>
  <action>
1. In queries.py, modify the PRODUCT_AREA_CASE constant (around line 37-49):
   - Change the line `WHEN event IN ('sweep_created', 'sweep_viewed') THEN 'Sweeps'`
   - Split into two lines:
     `WHEN event IN ('sweep_created') THEN 'Sweeps Created'`
     `WHEN event IN ('sweep_viewed') THEN 'Sweeps Viewed'`
   - This is the ONLY change to queries.py. The product_areas_query() function uses PRODUCT_AREA_CASE
     and already groups by product_area, so the split flows automatically through usage.py's
     _build_product_areas() which aggregates by area name.

2. In usage.js, fix seat utilization chart clipping (renderSeatChart function, around line 259):
   - Change grid from `{ left: 48, right: 80, top: 36, bottom: 32 }` to
     `{ left: 60, right: 24, top: 40, bottom: 40 }`
   - The right: 80 was excessive (leftover from markLine label). The top: 36 clips the
     y-axis max value label ("193"). Increase top to 40 for headroom.
   - Move the markLine label position to `position: 'insideEndTop'` so it doesn't need
     right margin space.

3. In usage.js, fix tracked hours chart clipping (renderHoursChart function, around line 531):
   - Change grid from `{ left: 72, right: 24, top: 24, bottom: 64 }` to
     `{ left: 72, right: 24, top: 24, bottom: 72 }`
   - The bottom: 64 clips rotated x-axis date labels. Increase to 72.

No changes to usage.py — it already aggregates product_areas by the area name string
returned from the CASE expression, so "Sweeps Created" and "Sweeps Viewed" will appear
as separate entries automatically.
  </action>
  <verify>
    <automated>grep -n "Sweeps Created" .claude/skills/bigquery/scripts/queries.py && grep -n "Sweeps Viewed" .claude/skills/bigquery/scripts/queries.py && grep -n "top: 40" .claude/skills/customer-snapshot/templates/panels/usage.js && grep -n "bottom: 72" .claude/skills/customer-snapshot/templates/panels/usage.js</automated>
  </verify>
  <acceptance_criteria>
    - queries.py contains `THEN 'Sweeps Created'` on one line and `THEN 'Sweeps Viewed'` on another
    - queries.py no longer contains `THEN 'Sweeps'` as a single combined area
    - usage.js seat chart grid has `top: 40` (was 36) and `left: 60` (was 48)
    - usage.js hours chart grid has `bottom: 72` (was 64)
    - usage.js seat chart markLine label has `position: 'insideEndTop'`
  </acceptance_criteria>
  <done>Sweeps split into created/viewed in BQ CASE, seat chart has room for top label, hours chart has room for bottom date labels</done>
</task>

<task type="auto">
  <name>Task 2: Split overlaid radar into two side-by-side charts</name>
  <read_first>
    .claude/skills/customer-snapshot/templates/panels/usage.js
  </read_first>
  <files>
    .claude/skills/customer-snapshot/templates/panels/usage.js
  </files>
  <action>
Replace the single renderRadarChart function (lines ~328-417) with two functions:
renderEventsRadar(chartEl, data) and renderUsersRadar(chartEl, data).

1. renderEventsRadar(chartEl, data):
   - Same sort/top-8 logic as current renderRadarChart
   - Single series: Events (blue color)
   - indicator max: Math.round(maxEvents * 1.2)
   - No legend needed (title in section label above)
   - Tooltip shows area name + event count
   - radar.radius: '65%', center: ['50%', '50%']

2. renderUsersRadar(chartEl, data):
   - Same sort/top-8 logic, same areas in same order (important for visual comparison)
   - Single series: Users (green color)
   - indicator max: Math.round(maxUsers * 1.2) — uses actual user scale, NOT scaled to events
   - No legend needed
   - Tooltip shows area name + unique user count
   - radar.radius: '65%', center: ['50%', '50%']

3. Update the render() function layout (around lines 716-724):
   - Replace the single radar section with a two-column grid containing both radars
   - Layout change: remove radar from the existing .usage-two-col (which pairs radar+weave)
   - New structure:
     ```
     <!-- Product Adoption: two radars side by side -->
     <div class="usage-two-col">
       <div class="usage-chart-section">
         <div class="chart-label">PRODUCT ADOPTION — EVENTS</div>
         <div id="usage-radar-events" style="width:100%;height:340px;"></div>
       </div>
       <div class="usage-chart-section">
         <div class="chart-label">PRODUCT ADOPTION — USERS</div>
         <div id="usage-radar-users" style="width:100%;height:340px;"></div>
       </div>
     </div>
     <!-- Weave gets its own full-width section (or pair with something else) -->
     ```
   - Weave chart becomes full-width (remove from two-col), height stays 240px.

4. Update render() chart init section (around lines 760-767):
   - Replace single radarEl init with two:
     var radarEventsEl = container.querySelector('#usage-radar-events');
     if (radarEventsEl) { var c = renderEventsRadar(radarEventsEl, data); if (c) charts.push(c); }
     var radarUsersEl = container.querySelector('#usage-radar-users');
     if (radarUsersEl) { var c2 = renderUsersRadar(radarUsersEl, data); if (c2) charts.push(c2); }

5. Remove the old renderRadarChart function entirely.
  </action>
  <verify>
    <automated>grep -c "renderEventsRadar\|renderUsersRadar" .claude/skills/customer-snapshot/templates/panels/usage.js | grep -q "^[2-9]" && grep -c "renderRadarChart" .claude/skills/customer-snapshot/templates/panels/usage.js | grep -q "^0$" && echo "PASS: dual radar functions exist, old removed"</automated>
  </verify>
  <acceptance_criteria>
    - usage.js contains function renderEventsRadar(chartEl, data)
    - usage.js contains function renderUsersRadar(chartEl, data)
    - usage.js does NOT contain function renderRadarChart (old single radar removed)
    - usage.js contains div id="usage-radar-events" and div id="usage-radar-users"
    - Events radar uses blue color (#60a5fa dark / #1565a0 light)
    - Users radar uses green color (#4ade80 dark / #1a7a4c light)
    - Both radars use the same sorted top-8 areas for visual comparison
    - Each radar has its own natural scale (events scale for events, user scale for users)
  </acceptance_criteria>
  <done>Product adoption radar split into two independently readable charts showing Events and Users separately, side by side in a two-column grid</done>
</task>

</tasks>

<verification>
1. `grep "Sweeps Created" .claude/skills/bigquery/scripts/queries.py` — confirms sweeps split
2. `grep "renderEventsRadar\|renderUsersRadar" .claude/skills/customer-snapshot/templates/panels/usage.js` — confirms dual radar
3. `grep -v "renderRadarChart" .claude/skills/customer-snapshot/templates/panels/usage.js | wc -l` — old function gone
4. Visual: open a generated dashboard and confirm two separate radar charts render
</verification>

<success_criteria>
- PRODUCT_AREA_CASE splits sweeps into created/viewed
- Two separate radar charts render for Events and Users
- Seat utilization top label not clipped
- Tracked hours bottom date labels not clipped
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-v2-ux-polish-data-provenance-split-charts-navigation-improvements/06-01-SUMMARY.md`
</output>
