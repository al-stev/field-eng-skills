---
phase: 09-skill-audit-and-consolidation
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - .claude/rules/skill-composition.md
autonomous: true
requirements: [AUDIT-04]

must_haves:
  truths:
    - "skill-composition.md documents the v2 dashboard generation pipeline (assemble -> compose -> dashboard folder)"
    - "skill-composition.md references coreweave.atlassian.net throughout (no wandb.atlassian.net)"
    - "skill-composition.md includes the Lattice Weekly Update workflow"
    - "All existing workflows still accurately describe current skill behavior"
  artifacts:
    - path: ".claude/rules/skill-composition.md"
      provides: "Complete multi-skill workflow documentation covering all composition patterns"
      contains: "Dashboard Generation"
  key_links:
    - from: ".claude/rules/skill-composition.md"
      to: ".claude/skills/customer-snapshot/scripts/assemble.py"
      via: "Dashboard Generation workflow references assemble.py pipeline"
      pattern: "assemble"
    - from: ".claude/rules/skill-composition.md"
      to: ".claude/skills/customer-snapshot/scripts/compose.py"
      via: "Dashboard Generation workflow references compose.py assembly"
      pattern: "compose"
    - from: ".claude/rules/skill-composition.md"
      to: ".claude/skills/lattice/SKILL.md"
      via: "Lattice Weekly Update workflow references lattice skill"
      pattern: "lattice"
---

<objective>
Update skill-composition.md to cover all multi-skill workflows including the new v2 dashboard generation pipeline and Lattice weekly updates, update Jira references to coreweave.atlassian.net, and verify all existing workflows still accurately describe current behavior.

Purpose: AUDIT-04 requires skill-composition.md to be complete and cover all multi-skill workflows including changes from Phase 7 (Jira migration) and Phase 8 (dashboard pipeline).
Output: Updated skill-composition.md with complete, accurate workflow documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-skill-audit-and-consolidation/09-CONTEXT.md
@.planning/phases/09-skill-audit-and-consolidation/09-01-SUMMARY.md
@.planning/phases/09-skill-audit-and-consolidation/09-02-SUMMARY.md
@.claude/rules/skill-composition.md
@.claude/skills/customer-snapshot/SKILL.md
@.claude/skills/lattice/SKILL.md
</context>

<interfaces>
<!-- Current skill-composition.md structure (12 existing workflows): -->
<!-- Issue Triage, Customer Lookup, Customer Snapshot, Communication Prep, -->
<!-- FE-UPDATE Workflow, Action Tracking, Programme Update, RAID Management, -->
<!-- Usage Report, Customer Onboarding, Customer Silence Check, Meeting Follow-Up, -->
<!-- Task Hygiene -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update skill-composition.md with new workflows and Jira migration fixes</name>
  <files>.claude/rules/skill-composition.md</files>
  <read_first>
    .claude/rules/skill-composition.md
    .claude/skills/customer-snapshot/SKILL.md
    .claude/skills/customer-snapshot/scripts/assemble.py
    .claude/skills/customer-snapshot/scripts/compose.py
    .claude/skills/lattice/SKILL.md
    .claude/skills/deep-analytics/SKILL.md
    .claude/skills/usage-report/SKILL.md
    SKILL-INVENTORY.md
  </read_first>
  <action>
    Update `.claude/rules/skill-composition.md` with three categories of changes. Be additive -- preserve existing workflow structure and add/modify, don't rewrite from scratch.

    **A. Add new workflow: Dashboard Generation (per D-09)**

    Add a new "## Dashboard Generation" section documenting the v2 dashboard pipeline. Read assemble.py and compose.py to get the actual current pipeline steps:

    ```
    ## Dashboard Generation

    Generate a v2 modular dashboard for a customer with all available data panels.

    1. **customer-snapshot** -- Orchestrates the full pipeline. Triggered by `/customer-snapshot CustomerName`.
    2. **jira** -- assemble.py fetches all open issues for the customer (issues.py list --customer X --with-comments).
    3. **slack** -- assemble.py fetches channel history and search results for sentiment analysis.
    4. **asana** -- assemble.py fetches SE action tasks from the customer's Asana project.
    5. **bigquery** -- assemble.py fetches usage metrics and runs deep-analytics transforms (cohort, risk, team, journey, decay, velocity, SDK, correlation, performance).
    6. **customer-snapshot (compose.py)** -- Assembles the dashboard folder: copies shell.html template, generates panel JS files from templates + data, writes data.js with dated history snapshots, bundles lib/ (ECharts, chart-helpers, panel-registry).
    7. Output: `customers/<name>/dashboard/` folder with index.html, data.js, panel JS files, lib/ -- opens in browser from file:// protocol.
    ```

    Adjust the numbered steps based on what you actually find in assemble.py and compose.py. The above is a starting template -- verify against the real code.

    **B. Add new workflow: Lattice Weekly Update**

    Add a new "## Lattice Weekly Update" section based on the lattice SKILL.md:

    ```
    ## Lattice Weekly Update

    Generate a weekly Lattice update by gathering activity across data sources and mapping to IC5 growth areas.

    1. **slack** -- Search for the user's messages and mentions across workspace channels (past 7 days).
    2. **asana** -- Fetch completed and in-progress tasks from the past week.
    3. **jira** -- Fetch issues the user commented on or transitioned in the past week.
    4. **gcalendar** -- Fetch meeting events from the past week (customer calls, internal meetings).
    5. **gong** -- Fetch call recordings and summaries from the past week (optional, degrades gracefully).
    6. **bigquery** -- Fetch any BQ queries run in the past week (optional, degrades gracefully).
    7. **lattice** -- Maps gathered activities to IC5 growth areas, generates Lattice-ready output for 4 fields + growth area scorecard.
    8. Output: Lattice-ready text to terminal + weekly archive file in `weekly-updates/`.

    Privacy: No Gmail search, no Slack 1:1 DMs.
    ```

    **C. Update existing workflows for Jira migration (per D-10)**

    Scan every workflow section for references to wandb.atlassian.net and replace with coreweave.atlassian.net. Based on current file content, the workflows use generic skill names (not URLs), so this may be a no-op. But verify by reading the full file.

    **D. Verify existing workflows still accurate (per D-11)**

    Read through each of the 12 existing workflows and verify they still describe current behavior. Key changes to check:
    - "Customer Snapshot" workflow: output path says `customers/<name>/trackers/YYYY-MM-DD-dashboard.html` -- this is STALE. Update to reference the new Dashboard Generation workflow or update the output path to `customers/<name>/dashboard/`.
    - "Usage Report" workflow: verify this still accurately describes the standalone usage-report skill (separate from the dashboard pipeline).
    - "Customer Lookup" step 0 references salesforce -- verify this is still accurate.

    If any existing workflow has stale information, fix it in place. If a workflow is fully obsolete, note it but don't delete (be additive per CLAUDE.md preferences).

    **E. Update the Customer Snapshot workflow**

    The existing "Customer Snapshot" workflow (lines 25-31) describes the old pipeline. Update it to reference the new v2 pipeline:
    - Step 4 should reference assemble.py + compose.py, not just "Generate HTML dashboard"
    - Step 5 output path should be `customers/<name>/dashboard/` (folder, not single HTML file)
    - Add a note that the dashboard includes 15 panels (6 operational + 9 analytics) when BQ data is available
  </action>
  <verify>
    <automated>grep "Dashboard Generation" .claude/rules/skill-composition.md && grep "Lattice Weekly Update" .claude/rules/skill-composition.md && grep -c "wandb\.atlassian\.net" .claude/rules/skill-composition.md | grep "^0$" && grep "compose.py\|assemble.py" .claude/rules/skill-composition.md</automated>
  </verify>
  <acceptance_criteria>
    - skill-composition.md contains a "Dashboard Generation" section documenting the v2 pipeline
    - skill-composition.md contains a "Lattice Weekly Update" section
    - Zero references to wandb.atlassian.net in skill-composition.md
    - "Customer Snapshot" workflow updated with v2 folder-based output path
    - All 12 original workflows preserved (none deleted)
    - compose.py and assemble.py referenced in Dashboard Generation workflow
  </acceptance_criteria>
  <done>skill-composition.md is complete with 14 workflows (12 original + Dashboard Generation + Lattice Weekly Update). All Jira references point to coreweave.atlassian.net. Customer Snapshot workflow updated for v2 pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Final cross-cutting verification of all AUDIT requirements</name>
  <files></files>
  <read_first>
    SKILL-INVENTORY.md
    .claude/rules/skill-composition.md
    CLAUDE.md
  </read_first>
  <action>
    Run a final verification pass across all four AUDIT requirements to confirm everything is satisfied:

    **AUDIT-01 check:** Verify SKILL-INVENTORY.md exists, has 35 skill rows, has entry-point/building-block/setup classification, has dependency graph section.
    ```bash
    # Count table data rows (excluding header and separator)
    grep -c "^|" SKILL-INVENTORY.md
    # Confirm classification terms present
    grep -c "entry-point\|building-block\|setup" SKILL-INVENTORY.md
    # Confirm dependency graph
    grep "consumed by" SKILL-INVENTORY.md
    ```

    **AUDIT-02 check:** Scan for hardcoded user-specific values in committed .py and .sh files.
    ```bash
    # Scan for Gong hardcoded fallback (should be gone)
    grep -rn "315301294163453491" .claude/skills/
    # Scan for hardcoded Gong URL fallback (should be gone)
    grep -rn "us-39259" .claude/skills/gong/scripts/gong_client.py
    # Verify Asana workspace GIDs are documented as acceptable
    grep "Hardcoded Value Audit" SKILL-INVENTORY.md
    ```

    **AUDIT-03 check:** Spot-check 3 SKILL.md files for standard sections.
    ```bash
    # Verify customer-snapshot has standard sections
    grep -c "## Purpose\|## Prerequisites\|## Usage\|## Output\|## Dependencies\|## Credentials" .claude/skills/customer-snapshot/SKILL.md
    # Verify jira has standard sections
    grep -c "## Purpose\|## Prerequisites\|## Usage\|## Output\|## Dependencies\|## Credentials" .claude/skills/jira/SKILL.md
    # Verify no SKILL.md references old Jira instance
    grep -rl "wandb\.atlassian\.net" .claude/skills/*/SKILL.md | wc -l
    ```

    **AUDIT-04 check:** Verify skill-composition.md has all workflows.
    ```bash
    # Count workflow sections (## headings)
    grep -c "^## " .claude/rules/skill-composition.md
    # Confirm new workflows present
    grep "Dashboard Generation" .claude/rules/skill-composition.md
    grep "Lattice Weekly Update" .claude/rules/skill-composition.md
    ```

    If any check fails, report the failure clearly with the specific grep command and output so it can be diagnosed. Do NOT attempt to fix issues in this task -- this is verification only.
  </action>
  <verify>
    <automated>grep -c "entry-point" SKILL-INVENTORY.md && grep -rn "315301294163453491" .claude/skills/ | wc -l | grep "^0$" && grep -rl "wandb\.atlassian\.net" .claude/skills/*/SKILL.md | wc -l | grep "^0$" && grep "Dashboard Generation" .claude/rules/skill-composition.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-01: SKILL-INVENTORY.md has 35 skills with classification and dependency graph
    - AUDIT-02: Zero hardcoded user-specific values in committed .py/.sh source (Gong fallbacks removed, Asana workspace constants documented as acceptable)
    - AUDIT-03: All SKILL.md files have standard sections, no stale Jira references, customer-snapshot reflects v2 pipeline
    - AUDIT-04: skill-composition.md has 14+ workflows including Dashboard Generation and Lattice Weekly Update
  </acceptance_criteria>
  <done>All four AUDIT requirements verified. Phase 9 success criteria met.</done>
</task>

</tasks>

<verification>
1. `grep -c "^## " .claude/rules/skill-composition.md` -- should be 14+ (12 original + 2 new)
2. `grep "Dashboard Generation" .claude/rules/skill-composition.md` -- workflow exists
3. `grep "Lattice Weekly Update" .claude/rules/skill-composition.md` -- workflow exists
4. `grep -c "wandb\.atlassian\.net" .claude/rules/skill-composition.md` -- should be 0
5. Full AUDIT-01 through AUDIT-04 verification passes (Task 2 output)
</verification>

<success_criteria>
- skill-composition.md has Dashboard Generation workflow documenting assemble.py -> compose.py -> dashboard folder pipeline
- skill-composition.md has Lattice Weekly Update workflow
- All Jira references in skill-composition.md point to coreweave.atlassian.net
- Customer Snapshot workflow updated for v2 folder-based output
- All 12 original workflows preserved and verified accurate
- Cross-cutting verification of AUDIT-01 through AUDIT-04 passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-skill-audit-and-consolidation/09-03-SUMMARY.md`
</output>
