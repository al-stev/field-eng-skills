---
phase: 09-skill-audit-and-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL-INVENTORY.md
  - .claude/skills/gong/scripts/gong_client.py
  - CLAUDE.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "SE can open SKILL-INVENTORY.md and find every skill with a one-line description"
    - "SE can see which skills are entry-points vs building-blocks"
    - "SE can trace which skills depend on which other skills"
    - "grep for known hardcoded GIDs in committed .py source returns only acceptable patterns (workspace-level constants, test fixtures)"
  artifacts:
    - path: "SKILL-INVENTORY.md"
      provides: "Complete skill inventory with classification and dependency graph"
      contains: "entry-point"
    - path: ".claude/skills/gong/scripts/gong_client.py"
      provides: "Gong client with configurable workspace ID (no silent hardcoded fallback)"
    - path: "CLAUDE.md"
      provides: "Project structure section listing all 35 skills including deep-analytics and lattice"
      contains: "deep-analytics"
  key_links:
    - from: "SKILL-INVENTORY.md"
      to: ".claude/skills/*/SKILL.md"
      via: "skill directory names match inventory rows"
      pattern: "entry-point|building-block"
    - from: "CLAUDE.md"
      to: ".claude/skills/*"
      via: "Project Structure section lists all skill directories"
      pattern: "deep-analytics|lattice"
---

<objective>
Publish a complete skill inventory (SKILL-INVENTORY.md) classifying all 35 skills as entry-point or building-block with dependency graph, scan and fix hardcoded user-specific values in committed source code, and update CLAUDE.md project structure to include missing skills.

Purpose: AUDIT-01 requires a published inventory so any SE can discover skills. AUDIT-02 requires no hardcoded user-specific values in committed code.
Output: SKILL-INVENTORY.md in repo root, cleaned gong_client.py, updated CLAUDE.md project structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-skill-audit-and-consolidation/09-CONTEXT.md
@CLAUDE.md
@.claude/rules/skill-composition.md
@templates/customers.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL-INVENTORY.md with classification and dependency graph</name>
  <files>SKILL-INVENTORY.md, CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    .claude/skills/3p-update/SKILL.md
    .claude/skills/asana/SKILL.md
    .claude/skills/asana-setup/SKILL.md
    .claude/skills/atlassian-setup/SKILL.md
    .claude/skills/bigquery/SKILL.md
    .claude/skills/bigquery-setup/SKILL.md
    .claude/skills/cadence-prep/SKILL.md
    .claude/skills/confluence/SKILL.md
    .claude/skills/credential-reference/SKILL.md
    .claude/skills/credential-status/SKILL.md
    .claude/skills/customer-setup/SKILL.md
    .claude/skills/customer-snapshot/SKILL.md
    .claude/skills/deep-analytics/SKILL.md
    .claude/skills/gcalendar/SKILL.md
    .claude/skills/gcalendar-setup/SKILL.md
    .claude/skills/gdocs/SKILL.md
    .claude/skills/gdocs-setup/SKILL.md
    .claude/skills/ghosted/SKILL.md
    .claude/skills/gmail/SKILL.md
    .claude/skills/gmail-setup/SKILL.md
    .claude/skills/gong/SKILL.md
    .claude/skills/gong-setup/SKILL.md
    .claude/skills/jira/SKILL.md
    .claude/skills/jira-check/SKILL.md
    .claude/skills/lattice/SKILL.md
    .claude/skills/maction/SKILL.md
    .claude/skills/nag/SKILL.md
    .claude/skills/pre-read/SKILL.md
    .claude/skills/raid/SKILL.md
    .claude/skills/rats/SKILL.md
    .claude/skills/salesforce/SKILL.md
    .claude/skills/salesforce-setup/SKILL.md
    .claude/skills/slack/SKILL.md
    .claude/skills/slack-setup/SKILL.md
    .claude/skills/usage-report/SKILL.md
  </read_first>
  <action>
    Create `SKILL-INVENTORY.md` in the repo root (per D-01). Structure:

    1. **Header** with title "Skill Inventory" and a brief intro explaining this lists all Claude Code skills.

    2. **Inventory table** with columns (per D-01): Skill Name, Description (one-line), Type (entry-point or building-block per D-02), Required Credentials, Invocation.
       - Read the `name` and `description` fields from each SKILL.md's YAML frontmatter for the one-liner.
       - Classification rules (per D-02):
         - "entry-point" = SE invokes directly via `/skill-name` (has a meaningful standalone use case)
         - "building-block" = consumed by other skills or composition workflows only
       - Entry-point skills: 3p-update, asana, cadence-prep, confluence, credential-reference, credential-status, customer-setup, customer-snapshot, deep-analytics, gcalendar, gdocs, ghosted, gmail, gong, jira, jira-check, lattice, maction, nag, pre-read, raid, rats, salesforce, slack, usage-report
       - Building-block skills: bigquery (consumed by customer-snapshot, usage-report, deep-analytics, lattice)
       - Setup skills (entry-point but infrequently invoked -- mark as "setup"): asana-setup, atlassian-setup, bigquery-setup, gcalendar-setup, gdocs-setup, gmail-setup, gong-setup, salesforce-setup, slack-setup
       - Required credentials: read from each SKILL.md or infer from the credential table in CLAUDE.md

    3. **Dependency graph** section showing which skills consume which. Use a descriptive list format (not mermaid -- simpler for grep/search):
       ```
       bigquery
         consumed by: customer-snapshot, usage-report, deep-analytics, lattice

       jira
         consumed by: customer-snapshot, jira-check, cadence-prep, pre-read, 3p-update, lattice

       slack
         consumed by: customer-snapshot, ghosted, cadence-prep, pre-read, 3p-update, lattice

       asana
         consumed by: customer-snapshot, ghosted, nag, maction, raid, 3p-update, lattice
       ```
       Build this by reading each SKILL.md's allowed-tools and dependency sections.

    4. **Also update CLAUDE.md project structure section** to add the two missing skill entries:
       - Add `deep-analytics/` with description: "Deep analytics HTML pages from BigQuery data (user journey, cohort, decay, velocity, team, risk, correlation, SDK, performance)"
       - Add `lattice/` with description: "Weekly Lattice update generator mapped to IC5 growth areas"
       - Place them in alphabetical order within the skills list.
  </action>
  <verify>
    <automated>grep -c "entry-point\|building-block\|setup" SKILL-INVENTORY.md | grep -v "^0$" && grep "deep-analytics" CLAUDE.md && grep "lattice" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL-INVENTORY.md exists in repo root
    - Table has exactly 35 rows (one per skill directory)
    - Every row has a Type value of "entry-point", "building-block", or "setup"
    - Dependency graph section lists at least bigquery, jira, slack, asana as consumed-by sources
    - CLAUDE.md project structure includes deep-analytics/ and lattice/ entries
  </acceptance_criteria>
  <done>SKILL-INVENTORY.md contains all 35 skills with classification, credentials, and dependency graph. CLAUDE.md project structure is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Scan and fix hardcoded user-specific values in committed source code</name>
  <files>.claude/skills/gong/scripts/gong_client.py</files>
  <read_first>
    .claude/skills/gong/scripts/gong_client.py
    .claude/skills/asana/scripts/asana_client.py
    .claude/skills/asana/scripts/mutate.py
    templates/customers.yaml
  </read_first>
  <action>
    Per D-03, "user-specific" means Asana GIDs, Slack channel IDs, email addresses, and user-specific URLs in committed .py and .sh source code. Per D-04, GIDs in SKILL.md and test fixtures are acceptable.

    **Scan:** Run grep across all committed .py and .sh files under .claude/skills/ for:
    - 13+ digit numeric strings (Asana GIDs, Gong workspace IDs)
    - Slack channel ID patterns (C[A-Z0-9]{10})
    - Email addresses (excluding @example.com and @test.com)
    - Hardcoded URLs to user-specific resources

    **Evaluate each finding** against the D-03/D-04 rules:

    1. `gong_client.py:45` -- `WORKSPACE_ID = ... or '315301294163453491'`
       This is a hardcoded Gong workspace ID. Per D-05, this needs to be configurable.
       **Fix:** Remove the hardcoded fallback. Make it raise an error if `GONG_WORKSPACE_ID` is not set in `~/.tsm-ai/.env`, similar to how GONG_COOKIE is handled. Change line 45 to:
       ```python
       WORKSPACE_ID = _load_credential('GONG_WORKSPACE_ID')
       ```
       Then in functions that use WORKSPACE_ID, add a check:
       ```python
       if not WORKSPACE_ID:
           raise ValueError("GONG_WORKSPACE_ID not found in ~/.tsm-ai/.env. Run /gong-setup to configure.")
       ```

    2. `gong_client.py:44` -- `GONG_BASE_URL = ... or 'https://us-39259.app.gong.io'`
       Same pattern -- hardcoded W&B Gong URL. Apply same fix: remove fallback, require env var `GONG_BASE_URL`.

    3. `asana_client.py:22` -- `_ASANA_WORKSPACE_GID_DEFAULT = "1208076155392173"`
       This is a **workspace-level** constant (same for all W&B SEs), documented in asana.md rules. It has an env var override mechanism. **Accept as-is** -- this is organization infrastructure, not user-specific per D-03.

    4. `mutate.py:47-55` -- Priority field GIDs and DEFAULT_TEAM_GID
       These are **workspace-level** Asana constants (same for all W&B SEs in the same workspace), documented in asana.md rules. **Accept as-is** -- organization infrastructure.

    5. Test fixtures (conftest.py, test_*.py) with example GIDs/emails -- **acceptable per D-04**.

    **Document findings** in the SKILL-INVENTORY.md (add a "Hardcoded Value Audit" section at the bottom) listing what was found, what was fixed, and what was accepted with rationale.
  </action>
  <verify>
    <automated>grep -n "or '315301294163453491'" .claude/skills/gong/scripts/gong_client.py | wc -l | grep "^0$" && grep -n "GONG_WORKSPACE_ID" .claude/skills/gong/scripts/gong_client.py</automated>
  </verify>
  <acceptance_criteria>
    - gong_client.py no longer contains hardcoded fallback '315301294163453491'
    - gong_client.py no longer contains hardcoded fallback 'https://us-39259.app.gong.io'
    - gong_client.py loads GONG_BASE_URL and GONG_WORKSPACE_ID from _load_credential() without fallbacks
    - gong_client.py raises ValueError with setup instructions when credentials are missing
    - SKILL-INVENTORY.md contains a "Hardcoded Value Audit" section documenting findings
    - Asana workspace GIDs in asana_client.py and mutate.py are left as-is (workspace-level, not user-specific)
  </acceptance_criteria>
  <done>All user-specific hardcoded values removed from committed source code. Workspace-level constants documented and accepted. Audit results recorded in SKILL-INVENTORY.md.</done>
</task>

</tasks>

<verification>
1. `ls SKILL-INVENTORY.md` -- file exists in repo root
2. `grep -c "|" SKILL-INVENTORY.md` -- table rows present (should be 35+ data rows)
3. `grep "deep-analytics\|lattice" CLAUDE.md` -- both skills listed in project structure
4. `grep -rn "315301294163453491" .claude/skills/gong/` -- returns nothing (hardcoded fallback removed)
5. `grep "Hardcoded Value Audit" SKILL-INVENTORY.md` -- audit section exists
</verification>

<success_criteria>
- SKILL-INVENTORY.md published with all 35 skills classified as entry-point/building-block/setup
- Dependency graph shows which skills consume which
- No hardcoded user-specific values remain in committed .py/.sh source (gong_client.py fixed)
- CLAUDE.md project structure includes deep-analytics/ and lattice/
- Audit trail documenting what was found and what was accepted
</success_criteria>

<output>
After completion, create `.planning/phases/09-skill-audit-and-consolidation/09-01-SUMMARY.md`
</output>
